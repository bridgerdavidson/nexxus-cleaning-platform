'use client';

// Placeholder content; the tenant overview (KPIs + roster) lands in the next task.
export default function PlatformOwnerTenantsPage() {
  return (
    <div className="mx-auto max-w-[1700px]">
      <h1 className="text-2xl font-bold text-foreground">Tenants</h1>
      <p className="mt-1 text-sm text-muted-foreground">Every cleaning company on Nexxus.</p>
    </div>
  );
}
