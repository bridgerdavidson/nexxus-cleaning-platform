# Committed defaults for unit tests. Safe stub values — never real secrets.
# Real values for integration tests go in .env.test.local (gitignored), written
# by the CI step that captures `supabase status --output json`.

NEXT_PUBLIC_SUPABASE_URL=http://stub.invalid
NEXT_PUBLIC_SUPABASE_ANON_KEY=stub-anon-key
SUPABASE_SERVICE_ROLE_KEY=stub-service-role-key
STRIPE_ENABLED=false
NEXT_PUBLIC_STRIPE_ENABLED=false
STRIPE_SECRET_KEY=sk_test_fake
STRIPE_WEBHOOK_SECRET=whsec_fake
