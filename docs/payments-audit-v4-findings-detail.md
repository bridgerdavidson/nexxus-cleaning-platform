# Money-code audit digest (run wf_87b1045f-50a, stopped early)
Domains completed: 7/7. Findings: 67 total, 37 confirmed, 1 refuted, 29 unverified.
Verdicts parsed: 38 (unmatched to a finding: 0)

================================================================================
## DOMAIN: charge-flow

COVERAGE: Read in full: src/lib/payments/chargeCompletedAppointment.ts, projectCompletionCharge.ts, presentChargeProjection.ts, moneyMath.ts, processingFee.ts, selfPayMath.ts, authorizeClient.ts, paymentSectionState.ts, settleCleanerPayout.ts, dispatchStripeEvent.ts (full), reconcile.ts (sweep structure, recoverStuckCharging, chargeUncollectedCompletions, settleUnsettledCaptures, checkMoneyMathInvariants); src/lib/stripe/charges/charge.ts, splits.ts; src/lib/stripe/setup-intents.ts and the createSetupIntent/getOrCreateStripeCustomer section of src/lib/stripe.ts; src/app/api/appointments/[appointmentId]/charge/route.ts and charge-projection/route.ts; src/app/api/stripe/create-setup-intent/route.ts and confirm-setup-intent/route.ts; src/lib/auth/requireOrgAuth.ts and requireSelfOrOrgStaff.ts; supabase/migrations/109 and 111; UI: HomeownerPaymentRecovery.tsx, PaymentsNeedingAttentionSection.tsx (states/filters). SOLID: the core split math (splits.ts) is correct and conservation-safe (floor cleaner, cap fee at tenant remainder, remainder absorbs rounding; invariant holds by construction); gross-up math (processingFee.ts) is exact including the ACH cap edge; the 1% fee cap and self-pay fee-on-top are implemented as designed and migration 111 is idempotent; double-charge protection is genuinely strong (claim RPC 109 serializes all callers, distinct idempotency keys charge-/cancelfee-/tenant-payout-/cleaner-payout-, reauth_count key rotation only after terminal decline, alreadySettled guard, PRECONDITION_CODES restore semantics carefully avoid both triage-erasure and double-charge, recoverStuckCharging backstops hard crashes); route authorization is thorough (org membership + role, cleaner assignment check, fail-closed homeowner allowlist excluding self-pay/requires_action, manager Manage Payments gate, matching fences on the projection route); customer dedup explicitly never matches by email; payout snapshot/carve-out logic in settleCleanerPayout prevents percent-edit over/underpay and transfer-id repair prevents double transfer. Unit tests exist for splits, selfPayMath, processingFee, projectCompletionCharge, presentChargeProjection, moneyMath; the charge route has a co-located integration test.

VISIBILITY: Admin visibility is good for the failure modes the code stamps as failures: declined and 3DS-blocked completion charges set authorization_status failed/requires_action, emit an admin charge_failed notification (chargeCompletedAppointment.ts:646-667), and land in PaymentsNeedingAttentionSection with a retry + card-link action. Homeowner visibility for those same states is also good (failed banner, Pay now, Update card, bell per PR #161). The blind spots: (1) precondition bails (no_card, tenant_not_ready) on non-self-pay jobs set NO failed status and NO notification, so a completed-but-uncollectable job appears in no admin queue and the sweep retries it silently forever; a job with payment_method_id NULL is additionally excluded from the sweep and the card-link self-heal, making it collectible only by a staff member manually opening the booking. (2) Successful off-session charges are invisible to the homeowner unless they open the app: no notification event and no Stripe receipt_email, a dispute-risk gap for a charge that lands days after booking. (3) Money-math drift caught by checkMoneyMathInvariants is recorded only to the forensic payment_events ledger, not surfaced to any operator UI.

--- [HIGH] Completed jobs that bail on charge preconditions are invisible: no failed status, no notification, no admin queue entry  => CONFIRMED (high, severity=high)
    file: src/lib/payments/chargeCompletedAppointment.ts:295  category: visibility
    summary: A completion charge that bails pre-Stripe on the homeowner path (no_card when the saved method is gone with no default fallback, or tenant_not_ready) returns without writing a payments row, without any authorization_status change (PRECONDITION_CODES restores the pre-claim status, normally NULL), and without recordPaymentEvent or notifyChargeFailed; only the SELF-PAY path has recordSelfPayNoCard. PaymentsNeedingAttentionSection filters appointments to authorization_status IN ('failed','requires_action') only, so these jobs appear in no admin queue, and the reconcile sweep (chargeUncollectedCompletions) silently re-bails every cycle. Worse subcase: an appointment with payment_method_id NULL (pre-cutover booking, or card-save never completed) is excluded from the sweep entirely (reconcile.ts:309 requires payment_method_id NOT NULL) and from the setup_intent.succeeded self-heal (dispatchStripeEvent.ts:609 selects only requires_action/failed), and even homeowner Pay now 409s at chargeCompletedAppointment.ts:295, so nothing automated can ever collect it. The authorizeClient.ts:74-76 contract comment ('the row lands in Payments needing attention') is not delivered for these codes.
    scenario: Homeowner books before cutover (payment_method_id NULL) or deletes their only card after booking; cleaner marks the job completed; the completion charge returns no_card with a one-time toast nobody records; the job sits completed and unpaid indefinitely with no admin notification, no needing-attention entry, and (in the NULL-payment-method case) no sweep retry. The tenant's revenue for that job is silently never collected until someone happens to open the booking drawer.
    evidence: chargeCompletedAppointment.ts:295 `if (!appt.payment_method_id) return { ok: false, code: 'no_card', ... }` (also :303, :317-319, :348-350) with no recordPaymentEvent/notifyChargeFailed on this path, vs :676-691 recordSelfPayNoCard for self-pay; :178-182 restores priorStatus (NULL) for PRECONDITION_CODES; PaymentsNeedingAttentionSection.tsx:95 `.in
    verdict: Every cited code claim verified. Homeowner-path precondition bails (no_card at chargeCompletedAppointment.ts:295/:303/:317-319, tenant_not_ready at :348-350) write no payments row, no payment_events entry, and no notification (notifyChargeFailed only runs on real declines/3DS at :582/:636; only self-pay has recordSelfPayNoCard at :439/:462, and no homeowner-path event type even exists in notifications/eventTypes.ts). PRECONDITION_CODES (:72-79, :
    correction: Two overstatements, neither changing the verdict: (1) the state is pull-visible, not fully invisible - derivePaymentSectionState returns 'no_card' and both the operator booking drawer and the homeowner cleaning detail (HomeownerPaymentRecovery.tsx:66-70) render it with an Add-a-card action that sets

--- [HIGH] Settlement race: webhook can split the grossed-up charge (Stripe fee included) when it beats the charge route's payments-row write  => CONFIRMED (high, severity=high)
    file: src/lib/payments/settleCleanerPayout.ts:122  category: money-correctness
    summary: settleCleanerPayout distributes splitBaseCents = amount_received - processing_fee_cents, but processing_fee_cents is read from the payments row, which finishCharge writes only AFTER stripe.paymentIntents.create returns (chargeCompletedAppointment.ts:538-558). Stripe fires payment_intent.succeeded the moment the confirm succeeds, i.e. potentially before the create call even returns to the route. On a first-attempt charge (no prior failed row carrying the fee) a fast webhook finds no payments row, processingFeeCents resolves to 0, and splitBase becomes the full grossed-up charge: the cleaner gets their % of (base + Stripe fee) and the tenant remainder includes the fee, over-distributing by ~feeCents (about 3% of the charge) that Stripe actually consumed, shorting the platform balance. Retry attempts are protected only because the prior decline row carries processing_fee_cents.
    scenario: First completion charge for a $120 job with passthrough on: charge = $123.90, feeCents = 390. Webhook arrives ~400ms after PI success while the route is still writing appointments/payments. Settlement splits $123.90 instead of $120.00: 50% cleaner gets $61.95 instead of $60.00, tenant remainder is also inflated; platform balance ends ~$3.90 short per occurrence with no automated clawback. checkMoneyMathInvariants later flags the cleaner drift (recomputed from total_price) but only into the forensic payment_events ledger; the money already moved.
    evidence: settleCleanerPayout.ts:80-84 `let capturedTotalCents = capturedCents ?? 0` (webhook passes paymentIntent.amount_received, the grossed-up amount, dispatchStripeEvent.ts:236-241) and :122-125 `const processingFeeCents = Number((payRow ... )?.processing_fee_cents ?? 0); const splitBaseCents = Math.max(0, capturedTotalCents - processingFeeCents - refun
    verdict: Every link in the claimed chain verifies: createDestinationCharge confirms synchronously (charge.ts:48-49) so payment_intent.succeeded can be delivered before finishCharge writes the payments row (chargeCompletedAppointment.ts:540-546); the webhook route dispatches immediately with no young-event delay (webhook/route.ts:87-104); handlePaymentIntentSucceeded has no missing-row guard (only a refunded-status guard, dispatchStripeEvent.ts:147-165) an
    correction: The finding is accurate but understates the terminal loss. Beyond the initial over-split of ~feeCents, the race also leaves payments.transfer_amount null (the update at settleCleanerPayout.ts:180-187 runs before the row exists), so the settleUnsettledCaptures sweep (reconcile.ts:373-438) re-attempts

--- [MEDIUM] Charge projection ignores payout_model: hourly_external cleaner's cut is displayed but never paid  => REFUTED (high)
    file: src/app/api/appointments/[appointmentId]/charge-projection/route.ts:111  category: ui
    summary: The projection route selects only payout_percent from cleaner_profiles and feeds it straight into projectCompletionCharge, but the actual settlement zeroes the cleaner share when payout_model = 'hourly_external' (settleCleanerPayout.ts:149-152), and the self-pay charge path refuses such cleaners outright (chargeCompletedAppointment.ts:445-449 cleanerPayable). The Complete sheet therefore shows an authoritative-looking cleaner cut (and correspondingly reduced tenant remainder) that the money movement will not honor.
    scenario: Org sets a cleaner to hourly_external but their profile retains payout_percent = 50. On job completion the Complete sheet projects a $60 cleaner cut on a $120 job; at settlement the cleaner receives $0 via Stripe and the tenant keeps the full remainder. Operator/cleaner expectations set by the sheet diverge from actual payout; on self-pay the projection shows numbers for a charge that then 409s cleaner_not_payable.
    evidence: charge-projection/route.ts:84-88 selects only `'payout_percent'`; :111-116 `payoutPercent = cleanerProfile?.payout_percent != null ? Number(...)` with no payout_model check, vs settleCleanerPayout.ts:149-152 `cleanerHasShare = ... cleaner.payout_model !== 'hourly_external' && Number(cleaner.payout_percent) > 0` and chargeCompletedAppointment.ts:445
    verdict: The code asymmetry the auditor cites is accurately described: charge-projection/route.ts:84-88 selects only payout_percent and lines 111-116 feed it into projectCompletionCharge with no payout_model check, while every real money path zeroes or refuses hourly_external cleaners (settleCleanerPayout.ts:149-152, chargeCompletedAppointment.ts:444-456, chargeSelfPayAchAppointment.ts:119, settleSelfPay.ts:132). However, the failure scenario ("Org sets a
    correction: Not a live defect, but a valid pre-launch note for the hourly_external feature: when that payout model is enabled (currently blocked at api/organizations/[orgId]/profile/route.ts:21 and with no write path to cleaner_profiles.payout_model), charge-projection/route.ts must add the same payout_model ch

--- [MEDIUM] Self-pay homeowner sees 'Payment failed' + a Pay now button that always 403s  => CONFIRMED (high, severity=medium)
    file: src/components/redesign/homeowner/cleanings/HomeownerPaymentRecovery.tsx:110  category: ui
    summary: The homeowner payment section hardcodes isSelfPay: false (the useHomeownerData appointment shape lacks the column, acknowledged in the comment). A self-pay appointment whose company-card charge declined has authorization_status = 'failed' (recordChargeDecline writes it regardless of self-pay), so derivePaymentSectionState returns 'failed' instead of 'self_pay': the homeowner gets the red 'Payment failed / We couldn't charge your card' alert and a Pay now button, but the charge route fail-closed allowlist rejects homeowners on self-pay (charge/route.ts:88-95 `!a.is_self_pay`), so the click always returns 403.
    scenario: Org runs a comped/self-pay cleaning for a homeowner; the company card declines at completion. The homeowner opens the cleaning detail, is told THEIR card failed (it was never involved), clicks Pay now, and gets 'Insufficient role for this action'. Support burden plus the homeowner may add a personal card believing they owe money.
    evidence: HomeownerPaymentRecovery.tsx:104-113 `isSelfPay: false, // The homeowner appointment shape (useHomeownerData) doesn't carry is_self_pay today`; paymentSectionState.ts:24-25 (self_pay short-circuit only when the flag is true, else 'failed'); chargeCompletedAppointment.ts:604 sets authorization_status 'failed' on self-pay declines too; charge/route.t
    verdict: Every link verified in code: recordChargeDecline (src/lib/payments/chargeCompletedAppointment.ts:604) unconditionally writes authorization_status='failed' for self-pay declines (called with isSelfPay:true from chargeSelfPayNow's catch at :493-498); HomeownerPaymentRecovery.tsx:110 hardcodes isSelfPay:false so derivePaymentSectionState (src/lib/payments/paymentSectionState.ts:24-25) returns 'failed' instead of 'self_pay', rendering the red "Paymen
    correction: The finding's stated root cause is outdated: the useHomeownerData appointment shape DOES carry is_self_pay now (type at src/hooks/useHomeownerData.ts:40, selected at :163-164, with a comment explicitly saying payment alerts must exclude self-pay rows). The component's line-107 comment claiming the c

--- [MEDIUM] No homeowner notification or Stripe receipt for the successful off-session completion charge  => CONFIRMED (high, severity=medium)
    file: src/lib/stripe/charges/charge.ts:43  category: notification
    summary: createDestinationCharge sets no receipt_email and finishCharge's success branch records only forensic payment_events ('charged'); recordNotificationEvent fires only for failures (notifyChargeFailed) . The homeowner's card is charged off-session, possibly days after booking and without them opening the app, with no in-app notification, no email, and no Stripe receipt (automatic receipts require receipt_email or a dashboard setting the code does not rely on). Unrecognized off-session charges are a classic dispute trigger, and disputes hit the tenant as merchant of record plus the $15 fee.
    scenario: Homeowner books Monday, the job completes Friday while they are offline; $123.90 appears on their statement with only Stripe's default descriptor. They don't recognize it, file a chargeback, and the tenant eats the dispute + fee, when a receipt/notification at charge time would have prevented it.
    evidence: charge.ts:43-67 PaymentIntentCreateParams contains amount/customer/payment_method/off_session/confirm/on_behalf_of/transfer_group/metadata, no receipt_email; chargeCompletedAppointment.ts:538-558 success path calls only recordPaymentEvent (forensic), while recordNotificationEvent appears only in notifyChargeFailed (:647-667) and recordSelfPayNoCard
    verdict: Verified. charge.ts:43-63 sets no receipt_email (zero hits repo-wide), and finishCharge's succeeded branch (chargeCompletedAppointment.ts:538-558) records only a forensic payment_events row. All recordNotificationEvent sites in the payments layer are failure- or admin-facing (notifyChargeFailed :647-667 is audience:'admin'; dispatchStripeEvent.ts sites at 381/415/530/814/1099 are authorization_failed/refund_failed/dispute_opened/cleaner_paid). Th
    correction: Two narrative corrections: (1) the statement will NOT show "Stripe's default descriptor" — because the charge sets on_behalf_of the tenant (charge.ts:53), the tenant cleaning company's own statement descriptor appears, so the homeowner sees the business they hired, which meaningfully lowers (but doe

--- [LOW] Recorded application_fee_amount ignores the cleaner-cut cap and can overstate the retained platform fee  => CONFIRMED (high, severity=low)
    file: src/lib/payments/chargeCompletedAppointment.ts:358  category: money-correctness
    summary: The homeowner charge row snapshots the platform fee via computePaymentSplit({grossCents: baseCents, payoutPercent: 0, platformFeeBps}), which caps the fee only at the full gross, while actual settlement recomputes with the real payoutPercent and caps at gross - cleanerCents (splits.ts:77). When payoutPercent + bps exceed 100% of gross (e.g. a 100%-payout cleaner at 1% bps), payments.application_fee_amount records a fee the platform never retains; a partial pre-settlement refund similarly shrinks the actual fee below the snapshot. Ledger/reporting accuracy only, no money moves wrong.
    scenario: Cleaner at payout_percent 100 on a $120 job: payments.application_fee_amount records 120 cents, settlement retains 0 (fee capped at tenant remainder of 0). Any revenue reporting or fee reconciliation summing application_fee_amount overstates platform income.
    evidence: chargeCompletedAppointment.ts:357-358 `const { platformFeeCents } = computePaymentSplit({ grossCents: baseCents, payoutPercent: 0, platformFeeBps })` written to the row at :393-394, vs settleCleanerPayout.ts:160-164 recomputing with real payoutPercent and splits.ts:77 `Math.min(platformFeeCentsFor(...), grossCents - cleanerCents)`.
    verdict: Confirmed. chargeCompletedAppointment.ts:357-358 snapshots the platform fee with payoutPercent: 0, which makes the splits.ts:77 cap (min(fee, gross - cleanerCents)) a no-op, and writes that uncapped 1%-of-gross to payments.application_fee_amount (:393). Settlement (settleCleanerPayout.ts:158-164) recomputes with the real payoutPercent on splitBaseCents (captured minus processing fee minus pre-settlement refunds), so the actually-retained fee is c
    correction: The finding is accurate but slightly over-broad in one implication: the same payoutPercent: 0 pattern in chargeCancellationFee.ts:133 is actually consistent (settlement never pays the cleaner on a cancelled job, settleCleanerPayout.ts:136), and the self-pay row is fee-on-top by design and matches it

--- [LOW] Sub-minimum charge amounts (base $0-$0.49) fail into a permanently failed state with no distinct signal  => CONFIRMED (high, severity=low)
    file: src/lib/payments/chargeCompletedAppointment.ts:352  category: money-correctness
    summary: baseCents = Math.round(Number(total_price) * 100) has no floor: a $0 appointment produces a 0-amount PaymentIntent create and a 1-49 cent price grosses up to below Stripe's 50-cent minimum; Stripe rejects either, recordChargeDecline stamps authorization_status 'failed', and every retry (operator, homeowner, card-link self-heal) fails identically since the amount never changes. The row at least lands in the needing-attention queue, but the 'retry with a working card' remediation it offers can never succeed. A NULL total_price makes baseCents NaN and computeChargeBreakdown throws (500; claim released safely).
    scenario: Operator creates a $0.25 touch-up appointment; at completion the charge is rejected by Stripe's minimum, the job shows 'Card charge failed', the admin sends a card link, the homeowner saves a new card, the auto-recharge fails again, looping the failure with no explanation that the amount itself is unchargeable.
    evidence: chargeCompletedAppointment.ts:352 `const baseCents = Math.round(Number(appt.total_price) * 100);` with no minimum validation before createDestinationCharge (:364); processingFee.ts:55-61 grossUpForFee(card, 25) = ceil(55/0.971) = 57 cents < Stripe's 50-cent minimum is fine, but grossUpForFee(card, 10) = 42 cents is not; charge failure path :373-379
    verdict: The core defect is real and I could not refute it: chargeCompletedAppointment.ts:352 has no amount floor, createDestinationCharge (src/lib/stripe/charges/charge.ts:43-67) passes the amount to Stripe unguarded, and the sub-minimum price is genuinely reachable because the edit-details route (src/app/api/appointments/[appointmentId]/details/route.ts:36) validates priceOverrideTotal >= 0, explicitly permitting $0 and sub-$0.50 prices. On Stripe rejec
    correction: Real but overstated in two places. (1) Drop the NULL total_price / NaN clause: appointments.total_price is NOT NULL in the DB (000_baseline.sql:827), so that branch is unreachable from real rows. (2) Fix the failing band: with prod's fee passthrough ON, the unchargeable range is base $0.00 plus roug

--- [LOW] confirm-setup-intent binds a first-save profile to any succeeded SetupIntent's customer without provenance check  => CONFIRMED (high, severity=low)
    file: src/app/api/stripe/confirm-setup-intent/route.ts:85  category: authz
    summary: The customer-match guard runs only when the profile already has stripe_customer_id; on first save (existingCustomerId null) any succeeded setup_intent_id is accepted, its payment method attached and set default, and the profile permanently bound to that SetupIntent's customer (the guard then locks it in forever). The SetupIntent's metadata is never checked against the homeowner. Exploitation requires knowing another customer's succeeded seti_ id (unguessable in normal flows, but seti ids appear in Stripe redirect return URLs and logs), so this is defense-in-depth rather than an open hole; a mistake here charges the wrong person's card on every future completion charge for that homeowner.
    scenario: Homeowner A (no stripe_customer_id yet) posts a leaked/mistaken setup_intent_id belonging to customer X; A's profile records stripe_customer_id = X; A's future bookings save/charge cards on X's customer, and chargeHomeownerNow's paymentMethodBelongsToCustomer check passes because the method genuinely belongs to X.
    evidence: confirm-setup-intent/route.ts:83-90 `const existingCustomerId = ... ?? null; if (existingCustomerId && existingCustomerId !== customerId) { return ... 409 }` — no else-branch validation (no metadata check; createSetupIntent in lib/stripe.ts sets only `metadata: { source: 'nexxus-cleaning-platform' }`), then :93 attaches and :96-100 persists the cus
    verdict: Verified against the actual code. confirm-setup-intent/route.ts:83-90 only runs the customer-match guard when the profile already has a stripe_customer_id; on first save (existingCustomerId null) it accepts any succeeded SetupIntent, attaches its PM (:93) and persists its customer (:96-100), permanently binding the profile. There is genuinely no provenance link to catch a mismatched seti: createSetupIntent (stripe.ts:110) and getOrCreateStripeCus
    correction: Failure scenario is accurate but slightly understates one mitigation: the standard UI flow (create-setup-intent then confirm-setup-intent) pre-populates the profile's stripe_customer_id before confirm executes, so the guard would fire. The vulnerable null window is only reachable by bypassing create


================================================================================
## DOMAIN: homeowner-ui-notifications

COVERAGE: Read in full: all five receipts components (HomeownerPaymentHistory/View, PaymentRow, PaymentReceipt, PaymentReceiptHost, derive-payments.ts), HomeownerPaymentRecovery.tsx, HomeownerCleaningDetail.tsx, CancelCleaningSheet.tsx, HomeownerPaymentAlerts.tsx + derivePaymentAlerts.ts, HomeownerHome wiring, BookingFlow/BookingReviewView/CardPickerSheet/deriveBooking/useSubmitBookingRequest, NotificationBell.tsx + navigation.ts + labels.ts + eventTypes.ts + recordEvent.ts + context.ts, email layer (sendEmail.ts, transport.ts, cardLinkEmail.ts, the card-links route), and the money paths that emit notifications: chargeCompletedAppointment.ts, chargeCancellationFee.ts, refundCancelledCharge.ts, dispatchStripeEvent.ts (payment_failed/processing/refund/setup_intent handlers), the cancel route, the charge route, the payment-method route, useCancelMyCleaning, useHomeownerData (appointments + payments hooks), paymentSectionState.ts, processingFee.ts, plus RLS migrations 075/088/089. SOLID: authz on the homeowner Pay now route (fail-closed allowlist, charge/route.ts:81-96) and payment-method route (owns-appointment + paymentMethodBelongsToCustomer); the atomic charge claim + idempotency around Pay now double-clicks; payments RLS after 089 (homeowner sees only own-appointment payments, staff org-scoped); the cancel sheet's fee disclosure and its fail-closed policy-load error handling; recordEvent dedupe_key idempotency (migration 088 unique index); cardLinkEmail escaping + SMTP header sanitization + requireTLS; the booking review's fee-inclusive total. The findings below are the gaps around those solid cores.

VISIBILITY: Homeowner: in-app PULL visibility of failed card charges is genuinely good (home-page alert banner + critical recovery card with Pay now/Update card + failed receipt row), but it is 100% pull-based in this branch: not one money event writes a homeowner-recipient notification_events row (every payments-lib emit is audience:'admin'; the 'authentication_required' homeowner bell label is dead code with no emit site), and the app sends exactly one email ever (the operator-initiated card link, sole sendEmail call site card-links/route.ts:186). A homeowner who does not open the app never learns their post-service charge failed. Post-booking amount disclosures (Pay now button, "You'll be charged", Paid heading/toast, urgent email) all quote the base price while the actual charge is grossed up for the processing fee, so the receipt won't match what they authorized. Receipts show the true charged amount but cannot distinguish a cancellation fee (or a company-funded self-pay charge) from a normal cleaning payment. Operator/admin: card-path failures are well covered (charge_failed / cancellation_fee_failed / refund_failed / dispute_opened / clawback_blocked bells all route to the Payments tab), but ACH pre-settlement failures for homeowner payers notify nobody and set no triage state, and no success-path money event (charge succeeded, fee collected, retry recovered) notifies anyone, which is acceptable for volume but means silent recovery/collection is invisible outside the payments list.

NOTIFICATION MATRIX:
MONEY EVENT -> HOMEOWNER / OPERATOR (this branch; bell = notification_events row, email = SMTP send)
1. Card saved at booking (self-service): homeowner: nothing (own action; card in Payment methods). Operator: nothing. Notifies NOBODY (acceptable).
2. Card saved via card link: homeowner: own action. Operator: homeowner_payment_links row flips 'completed' via realtime in triage UI (dispatchStripeEvent.ts:568-576); no bell. Side effect: stuck appointments auto re-pointed + completed ones auto re-charged.
3. Charge succeeded (completion, incl. homeowner Pay now): homeowner: NO bell, NO email receipt; receipt row appears in Receipts; detail shows Paid; toast only if they pressed Pay now themselves. Operator: NO bell; payments list + payment_events ledger only. Notifies NOBODY.
4. Charge failed (card decline / off-session 3DS): homeowner: NO bell, NO automatic email; home banner + critical recovery card IF they open the app (derivePaymentAlerts keys on authorization_status). Operator: bell 'charge_failed' -> Payments tab (chargeCompletedAppointment.ts:653); may manually send the urgent card-link email (the only email in the system).
5. Charge failed (ACH pre-settlement bounce, flag-gated): homeowner: NOTHING (no banner, section falls to before_charge). Operator: NO bell unless self_pay metadata (dispatchStripeEvent.ts:413); Failed pill in payments list only. Notifies NOBODY.
6. Late ACH return after settle/payout: operator: bell 'authorization_failed' + clawback (dispatchStripeEvent.ts:380-387). Homeowner: nothing; receipt stays 'paid'.
7. Retry succeeded (operator retry / homeowner Pay now / card-link auto re-charge): homeowner: banner/alert silently disappears; toast only if self-initiated. Operator: nothing. Notifies NOBODY.
8. Refund issued: homeowner: NO bell/email; receipt badge silently flips to 'refunded'. Operator: bell 'cancelled_job_refunded' ONLY for the auto-refund of an in-flight debit after cancel (refundCancelledCharge.ts:120); manual refunds are their own action.
9. Refund failed: operator: bell 'refund_failed' (dispatchStripeEvent.ts:530). Homeowner: nothing; their receipt may silently flip refunded->paid.
10. Cancellation fee charged: homeowner: pre-disclosed in CancelCleaningSheet ("Cancel and pay $X"); afterwards only a generic receipt row not labeled as a fee; NO bell/email. Operator: NO bell on success; payments row only. Notifies NOBODY post-hoc.
11. Cancellation fee failed/uncollectable: operator: bell 'cancellation_fee_failed' (chargeCancellationFee.ts:67). Homeowner: NOTHING; the sheet closed as success after a button that said "Cancel and pay $X".
12. Dispute opened: operator bell 'dispute_opened'. Cleaner payout settled: cleaner bell 'cleaner_paid'.
Events that notify NOBODY: #3 charge succeeded, #5 homeowner ACH failure, #7 retry recovered, #10 fee collected. #5 is the dangerous one (a failed money state); #3/#7/#10 are receipt-expectation gaps.

--- [HIGH] Every post-booking homeowner amount (Pay now button, charge copy, Paid toast, urgent email) quotes the base price while the card is actually charged the fee-grossed-up total  => UNVERIFIED (verifier never ran)
    file: src/components/redesign/homeowner/cleanings/HomeownerPaymentRecovery.tsx:101  category: money-correctness
    summary: With STRIPE_FEE_PASSTHROUGH_ENABLED=true (prod today), the completion charge grosses the service price up for the Stripe fee (chargeCompletedAppointment.ts:352-356, e.g. $120.00 -> $123.89) and writes that gross to payments.amount (line 385), but HomeownerPaymentRecovery builds priceLabel from appointment.total_price (line 101) and uses it in the 'Pay now · $X' button (line 294), the 'You'll be charged $X after your cleaning is completed' copy (line 55), the 'Paid $X' heading (line 59) and the success toast (line 153). The urgent failed-payment email quotes the same base amount (card-links/route.ts:73). Only the booking review (BookingReviewView.tsx:103-118) and the final receipt show the real total, so the homeowner authorizes 'Pay now · $120.00' and is charged $123.89, and the receipt matches nothing they saw after booking.
    scenario: Homeowner with a $120 completed job whose first charge failed opens the cleaning detail, sees 'Pay now · $120.00', taps it, and their card is charged $123.89; the toast says 'Paid $120.00' while the receipt row and their card statement say $123.89. Dispute/chargeback risk plus a support burden ('you charged me more than the button said').
    evidence: HomeownerPaymentRecovery.tsx:101 `const priceLabel = formatUsd(appointment.total_price);` + :294 `Pay now · {priceLabel}` vs chargeCompletedAppointment.ts:354-356 `const { chargeCents, feeCents } = passthrough ? computeChargeBreakdown('card', baseCents) : ...` and :385 `amount: chargeCents / 100`; email: card-links/route.ts:73 `amountLabel: appt.to

--- [HIGH] No money event ever notifies the homeowner: a failed post-service charge produces no bell row and no automatic email  => UNVERIFIED (verifier never ran)
    file: src/lib/payments/chargeCompletedAppointment.ts:653  category: notification
    summary: Every notification emit in the payments layer is audience:'admin' (charge_failed at chargeCompletedAppointment.ts:653-666, cancellation_fee_failed at chargeCancellationFee.ts:67-73, refund_failed at dispatchStripeEvent.ts:530-536, cancelled_job_refunded at refundCancelledCharge.ts:120-126); grep for audience:'homeowner' finds zero payments-lib sites. The only email in the entire app is the operator-initiated card-link (sole sendEmail call, card-links/route.ts:186). The homeowner bell (mounted at HomeownerTopBar.tsx:46) therefore never shows a money event, and the 'authentication_required' homeowner label variant (labels.ts:256-264) is dead code with no emit site. The failed state is visible only if the homeowner opens the app (HomeownerPaymentAlerts banner + recovery card).
    scenario: A homeowner's card declines at job completion. They do not open the app that week. They receive nothing: no bell, no email, no SMS. The org is unpaid for completed work until an operator notices the charge_failed bell and manually sends a card-link email; if the operator also misses it, collection stalls indefinitely. (Master reportedly gained a homeowner charge-failed bell in PR #161; this branch does not contain it, so this fee-branch would regress it if merged/deployed standalone.)
    evidence: chargeCompletedAppointment.ts:660 `audience: 'admin',` inside notifyChargeFailed; grep 'audience: homeowner' across src/lib/payments returns nothing; sole sendEmail call site: src/app/api/billing/card-links/route.ts:186 `await sendEmail({ to: profile.email, ...message });`

--- [MEDIUM] Failed cancellation fee is silent success to the homeowner who just pressed 'Cancel and pay $X', and there is no retry surface for either party  => UNVERIFIED (verifier never ran)
    file: src/components/redesign/homeowner/cleanings/CancelCleaningSheet.tsx:103  category: money-correctness
    summary: The cancel route returns fee_outcome/fee_captured_cents (cancel/route.ts:220-226) but useCancelMyCleaning.ts:31-33 and CancelCleaningSheet.submit (lines 103-112) discard them: a declined fee card still closes the sheet as plain success after a button labeled 'Cancel and pay $30.00' (line 141). The homeowner believes they paid. Admins get the cancellation_fee_failed bell, but the homeowner UI has no way to retry the fee: HomeownerPaymentRecovery's Pay now requires status==='completed' (line 222) and the charge route's homeowner allowlist requires a.status==='completed' (charge/route.ts:90-91), so a cancelled appointment can never be self-paid; the only in-code retry is re-POSTing the cancel route, which has no UI.
    scenario: Homeowner late-cancels a $200 cleaning with a 25% fee; their card declines the $50 fee off-session. Sheet closes with no message; homeowner believes they paid $50. A 'failed' $50 receipt row later appears labeled with the service name (not 'cancellation fee'). The org's $50 is uncollectable through the product; the admin bell points at a fee neither party has a button to collect.
    evidence: CancelCleaningSheet.tsx:106-108 `await cancel(appointment.id); onOpenChange(false); onCancelled();` (fee outcome ignored) vs cancel/route.ts:223-224 `fee_captured_cents: outcome.feeCapturedCents, fee_outcome: outcome.code`; retry blocked by charge/route.ts:90 `a.status === 'completed' &&`

--- [MEDIUM] Homeowner ACH pre-settlement failure sets no authorization_status and notifies nobody: invisible failed money state (flag-gated)  => UNVERIFIED (verifier never ran)
    file: src/lib/payments/dispatchStripeEvent.ts:392  category: visibility
    summary: handlePaymentIntentPaymentFailed's pre-settlement branch only flips payments.status='failed' (lines 392-405) and emits the admin bell solely when metadata.self_pay==='true' (line 413). It never writes appointments.authorization_status, but every homeowner failure surface keys on authorization_status: derivePaymentAlerts.ts:31 filters `a.authorization_status === 'failed' || ... 'requires_action'` and paymentSectionState.ts:25-31 never maps paymentStatus==='failed' to the 'failed' state (falls through to 'before_charge'). Card declines are safe (the inline path stamps authorization_status at chargeCompletedAppointment.ts:591/604), but an ACH debit that is accepted then bounces before settling exists ONLY as a Failed pill in the admin payments list: no homeowner banner, no recovery-card alarm, no admin bell. STRIPE_ACH_ENABLED is off in prod, so this is a latent trap for the ACH launch.
    scenario: (With ACH on) A homeowner's $300 bank debit is submitted at completion (processing), then fails 2 days later. Webhook marks the payments row failed. Homeowner home shows no alert; the cleaning detail payment section shows the calm 'Card on file... Pay now' state, not 'Payment failed'; no admin bell fires. The org is unpaid and the only signal is a pill buried in the payments list.
    evidence: dispatchStripeEvent.ts:392-395 `.update({ status: 'failed' }).eq('stripe_payment_intent_id', ...)` with no appointments update; :413 `if (paymentIntent.metadata?.self_pay === 'true')` gating the only bell; derivePaymentAlerts.ts:31 `.filter((a) => a.authorization_status === 'failed' || a.authorization_status === 'requires_action')`

--- [MEDIUM] CONFIRMED known issue: HomeownerPaymentRecovery hardcodes isSelfPay:false although the hook now selects is_self_pay, so a comped self-pay homeowner sees a false 'Payment failed' and a Pay now that 403s  => UNVERIFIED (verifier never ran)
    file: src/components/redesign/homeowner/cleanings/HomeownerPaymentRecovery.tsx:110  category: ui
    summary: Still present at line 110 (`isSelfPay: false`), with a stale justifying comment claiming useHomeownerData doesn't carry is_self_pay; it does: the interface declares it (useHomeownerData.ts:40) and the query selects it (line 164), and the home-page banner already uses it correctly (derivePaymentAlerts.ts:30 filters `!a.is_self_pay`). So the detail's recovery card blames the homeowner for a declined COMPANY-card charge and offers 'Pay now', which the charge route correctly rejects with 403 via `!a.is_self_pay` in the homeowner allowlist (charge/route.ts:92) plus the defense-in-depth block (chargeCompletedAppointment.ts:411-413). Self-pay is OFF in prod, so this is latent; the fix is passing `!!appointment.is_self_pay`.
    scenario: (With self-pay on) An org comps a cleaning for a real homeowner; the COMPANY card declines at completion. The homeowner opens the cleaning: red 'Payment failed. We couldn't charge your card for this cleaning' with a 'Pay now · $150.00' button. Tapping it returns 403 'Insufficient role for this action', shown raw in the inline error. Homeowner is alarmed and blamed for a charge they never owed.
    evidence: HomeownerPaymentRecovery.tsx:107-110 `// The homeowner appointment shape (useHomeownerData) doesn't carry is_self_pay today ... isSelfPay: false,` vs useHomeownerData.ts:164 `is_self_pay,` in the select; charge/route.ts:92 `!a.is_self_pay` in the homeowner fail-closed allowlist

--- [MEDIUM] Homeowner receipts cannot distinguish a cancellation fee (or a company-funded self-pay charge) from a normal cleaning payment, and show no fee breakdown  => UNVERIFIED (verifier never ran)
    file: src/hooks/useHomeownerData.ts:439  category: visibility
    summary: useHomeownerPayments selects only id/amount/status/paid_at/created_at + service name (lines 437-454): charge_kind, is_self_pay and processing_fee_cents are never fetched, and PaymentReceipt renders only Amount/Status/Service/Date (PaymentReceipt.tsx:56-76). A cancellation-fee payment (charge_kind='cancellation_fee', chargeCancellationFee.ts:191) renders as '{Service} $50.00 Paid', identical to a full cleaning charge; the fee-passthrough receipt shows one grossed-up number with no base-plus-fee breakdown despite processing_fee_cents existing on the row (migration 082); and under self-pay, company-card payment rows for comped cleanings are RLS-visible to the homeowner via the appointment join (089_security_audit_rls_hardening.sql:53-58) and would appear in 'your receipts' as if the homeowner paid the company's internal amount.
    scenario: A homeowner cancels late (charged a $50 fee) and separately pays $123.89 for another cleaning. Their Receipts list shows two rows both labeled 'Deep Clean', one $50.00 one $123.89, neither explaining what it was: the $50 looks like a cleaning they never received, and the $123.89 doesn't match the $120 price they were quoted anywhere in the receipt.
    evidence: useHomeownerData.ts:439-452 select list (`id, amount, status, paid_at, created_at, appointment:appointments(...)`) with no charge_kind/processing_fee_cents/is_self_pay; PaymentReceipt.tsx:63-77 renders only Service/Date/Cleaning-date fields

--- [MEDIUM] Charge success and refunds are silent to the homeowner: no receipt email or bell for an off-session charge to their card, and a refund (or its failure) only silently flips a badge  => UNVERIFIED (verifier never ran)
    file: src/lib/notifications/eventTypes.ts:12  category: notification
    summary: The event catalog (eventTypes.ts:12-37) contains no homeowner-audience success events: nothing for 'you were charged $X' after the auto charge-at-completion (finishCharge, chargeCompletedAppointment.ts:538-558, records only the payment_events ledger), and nothing homeowner-facing for refunds: cancelled_job_refunded and refund_failed are admin-audience (refundCancelledCharge.ts:125, dispatchStripeEvent.ts:535). A homeowner whose card is charged off-session while they sleep gets no receipt notification of any kind (no email infrastructure beyond the card link), and if a refund fails after the payment silently showed 'refunded', their receipt flips back to 'paid' with no explanation (dispatchStripeEvent.ts:518-523).
    scenario: A cleaner completes the job at 5pm; the homeowner's card is charged $123.89 off-session. The homeowner's only evidence is their bank statement, since the app sent nothing; they see an unrecognized charge and dispute it. Separately, an operator refunds a payment, the homeowner's receipt shows 'refunded', the refund then fails at Stripe, and the receipt silently reverts to 'paid': only admins are told the customer never got the money.
    evidence: eventTypes.ts:23-37 catalog (no homeowner charge-success/refund event); finishCharge success branch chargeCompletedAppointment.ts:538-558 (ledger only, no recordNotificationEvent); dispatchStripeEvent.ts:535 `payload: { ...ctx, audience: 'admin', ... }` on refund_failed

--- [LOW] BookingReviewView shows the processing-fee line unconditionally, ignoring the fee-passthrough UI flag the legacy summary respects  => UNVERIFIED (verifier never ran)
    file: src/components/redesign/homeowner/booking/BookingReviewView.tsx:47  category: ui
    summary: BookingReviewView computes the fee-inclusive total straight from computeChargeBreakdown (line 47 via bookingTotal, deriveBooking.ts:45-48) and renders 'Processing fee' whenever feeUsd > 0 (lines 109-114), never consulting stripeFeePassthroughUiEnabled() (flags.ts:103-105); the legacy BookingTotalSummary.tsx:73 does check it. The server decides the real charge with the server-side flag (chargeCompletedAppointment.ts:353). Prod has passthrough on so amounts currently match, but any environment (or rollback) with the server flag off would quote homeowners a fee they are never charged, and the two flags can drift independently.
    scenario: STRIPE_FEE_PASSTHROUGH_ENABLED is rolled back to false during an incident but the review screen still shows 'Processing fee $3.89 / Total when completed $123.89'; the homeowner is actually charged $120.00. Over-quoting is the safe direction, but the booking disclosure is wrong and inconsistent with the receipt.
    evidence: BookingReviewView.tsx:47 `const total = service ? bookingTotal(service.base_price, state.method) : null;` and :109 `{paymentRequired && total.feeUsd > 0 && (... Processing fee ...)}` with no flag check, vs BookingTotalSummary.tsx:73 `const feeEnabled = stripeFeePassthroughUiEnabled();`

--- [LOW] Cancel-sheet fee preview is frozen at open time while the server recomputes at confirm: a boundary-crossing cancel charges an undisclosed fee  => UNVERIFIED (verifier never ran)
    file: src/components/redesign/homeowner/cleanings/CancelCleaningSheet.tsx:86  category: money-correctness
    summary: The disclosure is computed client-side once from the client clock (computeCancellationFee in a useMemo, lines 86-99) while the cancel route independently recomputes the window server-side at confirm time (cancel/route.ts:142-155), and the response's fee_captured_cents is discarded (CancelCleaningSheet.tsx:103-112). A homeowner shown 'You can cancel this cleaning at no charge' (fee 0, button 'Cancel cleaning') who confirms after the window boundary passes (sheet left open, or client clock skew) is charged the fee with zero disclosure.
    scenario: Policy: 24h window, $50 fee. Homeowner opens the sheet 24h05m before the cleaning ('no charge'), gets distracted for six minutes, then taps 'Cancel cleaning'. The server computes insideWindow=true and charges $50 off-session; the sheet closes as success and nothing in the UI ever mentions the $50 until an unlabeled receipt row appears.
    evidence: CancelCleaningSheet.tsx:86-99 `const preview = useMemo(() => computeCancellationFee({ ... scheduledDate ... }), [...])` (client clock) vs cancel/route.ts:143-153 server-side computeCancellationFee; submit (lines 103-112) ignores the returned fee_captured_cents

--- [LOW] Receipt deep link shows 'Receipt not available / no longer on your account' while payments are still loading  => UNVERIFIED (verifier never ran)
    file: src/components/redesign/homeowner/account/receipts/PaymentReceiptHost.tsx:12  category: ui
    summary: PaymentReceiptHost has no loading state: with ?payment= set it immediately does payments.find(...) ?? null (lines 12-13) and PaymentReceipt renders the destructive-sounding empty state 'This receipt is no longer on your account' (PaymentReceipt.tsx:46-53) until useHomeownerPayments resolves. Any external/deep link (or cold-cache tab restore) into a receipt flashes a false 'your receipt is gone' message about a real payment.
    scenario: Homeowner opens a bookmarked receipt URL on a cold app load; for the first ~1s (or the full duration on a slow connection) the takeover asserts the receipt is no longer on their account, then swaps to the real $123.89 receipt. On a query error it asserts this permanently.
    evidence: PaymentReceiptHost.tsx:10-13 `const { payments } = useHomeownerPayments(); if (!paramId) return null; const payment = ... .find((p) => p.id === paramId) ?? null;` (loading/error from the hook unused)

--- [LOW] useHomeownerAppointments collapses multiple payments rows per appointment nondeterministically, which can flip the recovery card's state between refetches  => UNVERIFIED (verifier never ran)
    file: src/hooks/useHomeownerData.ts:197  category: ui
    summary: The payment-status backfill fetches all payments rows for the appointment ids with no .order() and reduces last-write-wins into paymentStatusMap (lines 197-206). Migration 088's partial unique index only constrains STRIPE-backed revenue rows; a manual record via /api/payments/record (payment_type defaults 'revenue', no PI, record/route.ts:56) can coexist with a Stripe row for the same appointment, so which status wins is row-order dependent. Since derivePaymentSectionState treats paymentStatus 'paid' as terminal (paymentSectionState.ts:27), a stale duplicate can make the homeowner detail flip between 'Paid' and its real state across refetches.
    scenario: An operator recorded a manual 'paid' cash row against an appointment that later also gets a failed Stripe charge attempt (authorization_status failed sorts first regardless, but for statuses below 'failed' precedence, e.g. paid-vs-pending) - the homeowner's payment section alternates between 'Paid $120.00' and 'Card on file' depending on which row the unordered query returns last.
    evidence: useHomeownerData.ts:197-205 `.in('appointment_id', appointmentIds)` with no order + `paymentStatusMap = payments.reduce((acc, p) => { acc[p.appointment_id] = p.status; return acc; }, ...)`; migration 088:66-68 unique index is partial: `WHERE payment_type = 'revenue'` AND stripe-backed only


================================================================================
## DOMAIN: operator-ui

COVERAGE: Read in full: OperatorPayments.tsx, OperatorPaymentsView.tsx, PaymentsTable.tsx, PaymentsCardList.tsx, PaymentDetailSheet.tsx, PaymentsTriageBand.tsx + usePaymentsTriage.ts, PaymentsKpiStrip.tsx + paymentsKpis.ts, PaymentsYourMoney.tsx, RecordPaymentDialog.tsx, payments-presenters.tsx, payments-types.ts, derivePayments.ts, derivePaymentsBadges.ts; bookings/payment/OperatorPaymentSection.tsx + ChangeCardSheet.tsx; overview/OperatorOverview.tsx, deriveOverview.ts, NeedsYouNowQueue.tsx, KpiStrip.tsx; PaymentStatusBadge.tsx (legacy, verified unused); src/lib/payments/paymentSectionState.ts; useAdminData.ts (AdminPayment/AdminPayout types, useAdminPaymentsInfinite, useAdminPayoutsInfinite, usePaymentStats, realtime subs); status enums (000_baseline.sql payment_status/payout_status, 082 adds 'processing'); payment_stats RPC (049 -> 077 latest, 089 grants); and the routes the UI drives: /api/payments/record, /api/payments/[id]/refund, /api/payouts/[id]/retry, /api/payouts/[id]/dismiss, /api/appointments/[id]/cancel; plus chargeCancellationFee.ts, chargeCompletedAppointment.ts (claim + alreadySettled), dispatchStripeEvent.ts (refund/dispute handlers), migrations 081 (realtime) and 088 (uniqueness). SOLID: status-enum coverage is complete in the redesign ledgers (all 5 payment statuses and all 6 payout statuses map to distinct labeled badges, no raw-text fallthrough; unknowns fall to sensible defaults); permission gating is consistent (can_view_payments gates the whole screen, can_manage_payments gates actions, refund is owner/admin-only, and all four money routes enforce the same server-side); realtime invalidation is wired for payments/payouts/refunds/disputes changes on the Payments screen; payout retry/dismiss are idempotent server-side and dismiss is UI-only (the sweep still retries); amounts are rendered from DB rows via one shared formatter (no client-side re-computation of splits); the triage band + overview queue both cover failed charges, and failed/held payouts are covered in the band with working retry/dismiss/message actions.

VISIBILITY: GOOD for the core flows, with real blind spots at the edges. The operator can see and act on: failed/requires-action charges (Payments triage band + Overview "Needs you now", with Fix card / Retry charge / Send card link), failed cleaner payouts (retry/dismiss), held payouts (grouped, message-cleaner), the full transaction and payout ledgers with complete badge coverage of every DB status, org Stripe balance/next payout (embedded ConnectPayouts), and revenue KPIs. Realtime keeps the Payments screen and triage live. The blind spots: (1) open disputes/chargebacks exist only as one transient bell notification - nothing on the Payments screen shows them, the disputed payment still reads "Paid", and the evidence deadline is displayed nowhere; (2) partial refunds and failed refunds are invisible in the ledger (payment stays "Paid" at full amount, refunds table is never read by any operator surface); (3) a failed cancellation/no-show fee is visible as a Failed ledger row + bell but has no retry action anywhere in the UI; (4) the triage band silently renders "all clear" when its queries error; (5) failed-charge visibility hangs entirely on the NEXT_PUBLIC charge-flow env mirror matching the server flag. Manual cash recording is the weakest correctness point: it does not clear the failed-charge state and can lead to a UI-encouraged double collection.

--- [HIGH] Recording a manual payment on a failed-charge booking leaves the failed state live and can double-collect via Retry charge  => CONFIRMED (high, severity=high)
    file: src/app/api/payments/record/route.ts:51  category: money-correctness
    summary: POST /api/payments/record inserts a paid revenue row but never clears appointments.authorization_status or checks for an existing revenue row. After a card decline (authorization_status='failed') an operator who collects cash and records it leaves the booking in the triage band and Overview queue with 'Retry charge'/'Fix card' actions. The idempotency guard in chargeCompletedAppointmentAuto (alreadySettled) selects the appointment's revenue rows with .limit(1) and NO .order(), and migration 088 deliberately lets a manual cash row coexist with the Stripe-backed failed row, so the guard can return the failed Stripe row, miss the paid manual row, and issue a real card charge on top of the collected cash. The route also has no already-paid guard at all, so double-recording inflates the revenue KPIs.
    scenario: Job completes, card declines (authorization_status='failed', Stripe revenue row status='failed'). Customer pays cash; operator uses Record payment (second revenue row, status='paid'). Booking stays in 'Needs you now' as a failed charge. Operator (or another admin) clicks Fix card -> Retry charge: alreadySettled's unordered limit(1) returns the failed Stripe row, returns null, and the card is charged for a job already paid in cash. Customer pays twice; only a manual refund recovers it.
    evidence: record/route.ts:51-67 builds paymentData and inserts with no appointment update and no existing-payment check; chargeCompletedAppointment.ts:200-211 alreadySettled: .eq('payment_type','revenue').limit(1) with no .order() and no status filter in the query; 088_payments_uniqueness_and_guards.sql header: 'Manual cash records (payments/record ... strip
    verdict: Verified end-to-end against the code. (1) record/route.ts:51-67 inserts a paid revenue row (stripe_payment_intent_id NULL) with no dedup check and never clears appointments.authorization_status. (2) Migration 088's unique index (lines 66-70) is partial on stripe_payment_intent_id IS NOT NULL, so the manual row coexists with the failed Stripe row, leaving two revenue rows. (3) alreadySettled (chargeCompletedAppointment.ts:200-211) uses .limit(1) w

--- [HIGH] Open disputes (chargebacks) have no operator surface beyond a one-time bell notification  => CONFIRMED (high, severity=high)
    file: src/lib/payments/dispatchStripeEvent.ts:783  category: visibility
    summary: charge.dispute.created upserts a disputes row (with amount, reason, evidence_due_by) and records a 'dispute_opened' notification that navigates to the Payments screen, but no operator UI reads the disputes table: the ledger row for the disputed payment still shows 'Paid', the triage band queries only appointments+payouts, and the evidence deadline is displayed nowhere. The realtime sub on disputes (useAdminData.ts:784-793) only invalidates lists that render no dispute data.
    scenario: A homeowner disputes a $300 charge. Admin gets one bell notification; tapping it lands on the Payments screen where nothing looks wrong (payment reads Paid, triage band is empty). The notification is marked read, the evidence_due_by passes unanswered, the dispute is lost by default, and the org eats the charge plus the $15 dispute fee with a cleaner clawback.
    evidence: dispatchStripeEvent.ts:783-825 writes disputes + recordNotificationEvent('dispute_opened'); navigation.ts:23 routes it to 'payments'; grep over src/components + src/hooks shows zero reads of from('disputes'); deriveTransactionBadge (derivePaymentsBadges.ts:5-18) is driven solely by payments.status, which a dispute never changes; usePaymentsTriage.t
    verdict: Verified. handleChargeDisputeCreated (src/lib/payments/dispatchStripeEvent.ts:755-826) upserts a disputes row and records a dispute_opened notification, but nothing in the app ever reads the disputes table: repo-wide grep shows only the webhook writer, two delete/wipe routes, and two realtime subs in useAdminData.ts (571-579, 785+) that merely invalidate payments queries which select no dispute data. payments.status is never changed by a dispute,
    correction: Two overstatements in the original: (1) the evidence deadline is not displayed "nowhere" - the notification detail line renders it once ("respond by {date}", src/lib/notifications/labels.ts:238-246, along with the amount); (2) the notification is not strictly one-time - it persists as a row in the N

--- [MEDIUM] Partial and failed refunds are invisible: payment renders 'Paid' at full amount and revenue KPIs overstate  => CONFIRMED (high, severity=medium)
    file: src/hooks/useAdminData.ts:688  category: visibility
    summary: The refund route supports partial refunds (payment stays status='paid' until cumulative coverage is full) and the system auto-issues refunds (refundCancelledCharge), all recorded in the refunds table - but PAYMENTS_INFINITE_SELECT never joins refunds, TransactionDetailVM has no refunded-amount field, and no operator surface reads refunds. A partially refunded payment shows as 'Paid' at the full original amount with an active Refund button. payment_stats (077) sums status='paid' at full amount, so total/this-month revenue KPIs overstate by every partial refund. When a refund later fails at Stripe, the payment is reverted to 'paid' with only a 'refund_failed' bell that navigates to a Payments screen showing no trace of the problem.
    scenario: $200 payment gets a $80 partial refund via the API (or an auto-refund path). Payments screen shows the row as Paid $200.00; Revenue KPIs count $200. A second admin opens the detail sheet, sees Paid + Refund, and has no way to know $80 already went back. Separately: a refund fails at Stripe, payment flips back to 'paid', and after the bell is dismissed there is zero UI evidence the customer was never refunded.
    evidence: refund/route.ts:83-87,144-147 (partial refunds; status stays 'paid' until fully covered); useAdminData.ts:688-709 select has no refunds join; payments-types.ts:52-58 TransactionDetailVM has no refund fields; 077_org_self_pay.sql:294-299 totalRevenue = sum(amount) where status='paid'; dispatchStripeEvent.ts:505-537 reverts 'refunded'->'paid' on refu
    verdict: Every evidence citation checks out. (1) Partial refunds keep status='paid': src/app/api/payments/[paymentId]/refund/route.ts:83-88 computes refundCents against gross minus prior pending/succeeded refunds, and lines 144-147 flip status to 'refunded' only when cumulative coverage reaches gross. (2) PAYMENTS_INFINITE_SELECT (src/hooks/useAdminData.ts:688-709) selects payments columns plus appointment/homeowner/service joins, no refunds join; a repo-

--- [MEDIUM] Failed cancellation/no-show fee has no retry action anywhere in the operator UI  => CONFIRMED (high, severity=medium)
    file: src/lib/payments/chargeCancellationFee.ts:218  category: manageability
    summary: A declined or 3DS-blocked cancellation fee writes a payments row status='failed' and an admin bell notification, but the appointment is already cancelled so it is excluded from both triage surfaces (usePaymentsTriage filters .neq('status','cancelled') and the fee path never sets authorization_status; deriveOverview's failedPayment also filters live(a)). The failed txn's detail sheet offers no action (only paid+card rows get Refund). The only retry path is re-POSTing /api/appointments/[id]/cancel (which is idempotent and re-attempts the fee with a bumped key), but no UI exposes cancel for an already-cancelled booking. The ledger row also lacks charge_kind in the select, so the failed fee is indistinguishable from a failed job charge.
    scenario: Homeowner late-cancels, the $50 fee card charge declines. Admin sees a bell + a Failed row in the ledger. They open the row: no action. They open the cancelled booking: no fee retry. The fee is permanently uncollected despite server-side retry machinery existing (attempt-bump logic at chargeCancellationFee.ts:135-142 is reachable only through a route the UI never calls again).
    evidence: chargeCancellationFee.ts:156-221 writes status='failed' rows + notifyFeeFailed, never touches appointments.authorization_status; usePaymentsTriage.ts:95 .neq('status','cancelled'); deriveOverview.ts:60-62 live(a) filter; PaymentDetailSheet.tsx:88-100 action rendered only when txn.refundable; cancel/route.ts:162-218 re-run is allowed and re-charges 
    verdict: Every cited claim verified against the code. chargeCancellationFee.ts:156-221 writes a payments row status='failed' (charge_kind='cancellation_fee') plus a cancellation_fee_failed admin notification, and never stamps appointments.authorization_status (its only appointments write is reauth_count at line 141; the webhook payment_failed handler at dispatchStripeEvent.ts:392-395 also only touches payments.status). Because the appointment is already c
    correction: The finding is accurate as stated. One nuance worth carrying forward: the state is not invisible — the admin gets a clearly-worded bell notification ("Cancellation fee not collected... Card declined", labels.ts:292-304) and a Failed ledger row — so this is a visible-but-unactionable manageability ga

--- [MEDIUM] Refund is a single un-confirmed click that issues a full refund  => CONFIRMED (high, severity=medium)
    file: src/components/redesign/payments/PaymentDetailSheet.tsx:95  category: money-safety-ui
    summary: The detail sheet's Refund button calls onRefund(txn.id) directly; OperatorPayments.handleRefund POSTs with no amount, which the route treats as a full refund of the remaining balance (and reverses the tenant + cleaner transfers). There is no confirmation dialog, no amount entry, and no partial-refund option, so a stray tap on a touch device irreversibly refunds the entire charge and claws back the cleaner.
    scenario: Admin opens a $450 paid transaction on mobile to check the reference and fat-fingers Refund. The full $450 goes back to the homeowner, the cleaner's payout is reversed, and the only recovery is manually re-collecting from the customer (no re-charge path exists for a refunded payment: refundable requires status 'paid').
    evidence: PaymentDetailSheet.tsx:91-98 Button onClick={() => onRefund(txn.id)} with no confirm; OperatorPayments.tsx:258-280 handleRefund body is only { organization_id } - no amount; refund/route.ts:83-84 'omit for full' semantics; refund/route.ts:155-162 reverses tenant and cleaner transfers.
    verdict: Confirmed by direct code read. PaymentDetailSheet.tsx:91-98 wires the Refund button straight to onRefund(txn.id) with no confirmation dialog anywhere in src/components/redesign/payments/ (grep for confirm/AlertDialog/window.confirm returned nothing; loading={busy} only blocks double-submit, not the first tap). OperatorPayments.tsx:258-267 POSTs only { organization_id }; refund/route.ts:83-84 treats a missing amount as a full refund of the remaini

--- [MEDIUM] Payments triage band silently shows 'all clear' when its queries fail  => CONFIRMED (high, severity=medium)
    file: src/components/redesign/payments/usePaymentsTriage.ts:151  category: visibility
    summary: reload() never inspects chargeRes.error / failedRes.error / heldRes.error; on any query failure (RLS regression, network, schema drift) the data arrays default to [] and setCharges/setFailedPayouts/setHeldPayouts are set to empty. PaymentsTriageBand returns null when isEmpty, and its absence is documented as 'the cockpit's all clear', so a broken triage query is indistinguishable from a healthy org. The Overview queue does surface query errors, but the Payments screen's band does not.
    scenario: A future RLS change breaks the payouts select for managers. Every failed payout disappears from the band with no error state; the operator sees the all-clear (band hidden) while failed transfers accumulate, discoverable only by manually filtering the payouts ledger to Failed.
    evidence: usePaymentsTriage.ts:86-111 Promise.all destructures results; lines 113, 129, 137 use (res.data ?? []) with no error branch; line 155 setLoading(false) unconditionally; PaymentsTriageBand.tsx:33 'if (t.isEmpty) return null;' and the docstring at lines 10-13.
    verdict: The core defect is verified: usePaymentsTriage.reload() (src/components/redesign/payments/usePaymentsTriage.ts:86-155) never inspects chargeRes.error/failedRes.error/heldRes.error; Supabase client queries return { data: null, error } without throwing, so any failure becomes (data ?? []) -> empty arrays -> isEmpty -> PaymentsTriageBand.tsx:33 returns null, whose absence the docstring (lines 10-13) defines as "all clear". No error state is ever set
    correction: Accurate as a code defect, but the failure scenario should be narrowed: an RLS regression affecting the whole payouts table would ALSO break the error-surfaced payouts ledger on the same screen (useAdminPayoutsInfinite throws -> hasError -> ErrorState "Couldn't load payments"), so the operator would

--- [MEDIUM] Failed-charge visibility hangs on the NEXT_PUBLIC charge-flow mirror matching the server flag  => CONFIRMED (high, severity=low)
    file: src/components/redesign/payments/usePaymentsTriage.ts:82  category: visibility
    summary: Both operator triage surfaces (Payments band and Overview queue) render failed/requires-action charges only when stripeNewChargeFlowUiEnabled() is true, i.e. NEXT_PUBLIC_STRIPE_NEW_CHARGE_FLOW_ENABLED === 'true' at build time - a separate env var from the server flag that actually produces the failures. If the mirror is unset, empty, or bit-rotted in a deploy (this exact failure mode already occurred in prod with the sibling STRIPE_TENANT_CONNECT_ENABLED stored as ''), the server keeps charging and failing while both triage surfaces render nothing - no error, just absence. A one-variable misconfig turns off the primary uncollected-money alarm.
    scenario: A Vercel env migration drops NEXT_PUBLIC_STRIPE_NEW_CHARGE_FLOW_ENABLED. Server flag still true: completions keep charging, some fail. The triage query is short-circuited client-side (chargesEnabled false -> Promise.resolve({data: []})) and OperatorOverview.tsx:98 empties failedPayment, so every failed charge is invisible in the UI until someone audits the ledger's Failed filter or Stripe directly.
    evidence: usePaymentsTriage.ts:82,86-97 chargesEnabled = stripeNewChargeFlowUiEnabled() gating the appointments query; OperatorOverview.tsx:97-105 same gate; flags.ts:55-57 reads process.env.NEXT_PUBLIC_STRIPE_NEW_CHARGE_FLOW_ENABLED === 'true'.
    verdict: The cited gating is real and verified: usePaymentsTriage.ts:82-97 short-circuits the failed/requires-action query and OperatorOverview.tsx:97-98 empties failedPayment when NEXT_PUBLIC_STRIPE_NEW_CHARGE_FLOW_ENABLED !== 'true' (flags.ts:55-57), a build-time mirror separate from the server flag. However, the finding's failure scenario ("every failed charge is invisible in the UI", "the primary uncollected-money alarm" turned off) is refuted by two 
    correction: Accurate statement: if NEXT_PUBLIC_STRIPE_NEW_CHARGE_FLOW_ENABLED drifts from the server flag, the Payments triage band and Overview queue stop listing failed/requires-action charges and their one-click recovery actions (Send card link / Fix card) disappear, degrading triage UX. Failed charges do NO

--- [LOW] Manual payments recorded with method 'card' show a Refund button that always fails  => CONFIRMED (high, severity=medium)
    file: src/components/redesign/payments/OperatorPayments.tsx:224  category: ui
    summary: refundable is derived purely as canRefund && status==='paid' && payment_method==='card', but RecordPaymentDialog lets an operator record a manual payment with method 'card' (and 'ach'), which the record route stores with no stripe_payment_intent_id. The detail sheet then shows an actionable Refund button whose POST always 409s with 'Payment is not in a refundable state'. Conversely, a settled ACH charge (method 'ach', status 'paid', has a PI) is refundable server-side but never shows the button.
    scenario: Operator records an external card-terminal payment choosing method Card. Weeks later another admin opens it and clicks Refund expecting Stripe to return the money; they get a generic 'Payment is not in a refundable state' error with no explanation, and may believe the refund system is broken.
    evidence: OperatorPayments.tsx:224 refundable: canRefund && p.status === 'paid' && p.payment_method === 'card'; RecordPaymentDialog.tsx:286-290 offers manual/card/ach; record/route.ts:51-61 stores no stripe_payment_intent_id; refund/route.ts:63-65 409s when !payment.stripe_payment_intent_id.
    verdict: Fully verified. OperatorPayments.tsx:224 derives refundable purely from canRefund && status==='paid' && payment_method==='card', with no stripe_payment_intent_id check, and PaymentDetailSheet.tsx:88-98 renders an actionable Refund button from that flag. RecordPaymentDialog.tsx:286-290 lets an operator record a payment with method 'card' or 'ach' (its own copy invites logging 'external' payments), and record/route.ts:51-61 stores it status='paid' 
    correction: Accurate as stated; only the severity is off. Raise low -> medium: the rubric classifies confusing UI states on money surfaces as medium, the trigger is the dialog's intended use (recording an external card payment), and the hidden ACH refund path becomes a live gap the day STRIPE ACH flags flip. Su

--- [LOW] payment_stats rounds revenue to whole dollars but tiles display cents  => CONFIRMED (high, severity=low)
    file: supabase/migrations/077_org_self_pay.sql:314  category: money-correctness
    summary: The payment_stats RPC returns round(v_total_revenue) / round(v_this_month_revenue) (numeric round to zero decimals), and paymentsKpiItems formats the result with money2 (two forced decimals). The KPI tiles therefore show e.g. $1,235.00 when the ledger sums to $1,234.56 - false precision that will not tie out against the transactions ledger or the Stripe balance during reconciliation (up to 50 cents drift per stat, plus the partial-refund overstatement noted separately).
    scenario: Owner cross-checks 'Total revenue $12,400.00' against an export of paid transactions summing $12,399.62 and burns time hunting a phantom 38-cent discrepancy created by the RPC's rounding.
    evidence: 077_org_self_pay.sql:313-317 jsonb_build_object('totalRevenue', round(v_total_revenue), ...); paymentsKpis.ts:10-13 formats with money2 (minimumFractionDigits: 2); usePaymentStats (useAdminData.ts:975-982) passes the rounded numbers straight through.
    verdict: Verified end-to-end; attempts to refute failed. (1) The RPC: supabase/migrations/077_org_self_pay.sql:313-316 returns jsonb_build_object('totalRevenue', round(v_total_revenue), 'pendingPayouts', round(v_pending_payouts), 'thisMonthRevenue', round(v_this_month_revenue)) — Postgres round(numeric) with no scale argument rounds to zero decimal places, i.e. whole dollars. I checked for a superseding definition: 077 is the LAST redefinition of payment_

--- [LOW] 'Approved' payout status renders a badge but has no ledger filter  => CONFIRMED (high, severity=low)
    file: src/components/redesign/payments/payments-types.ts:22  category: ui
    summary: payout_status includes 'approved' (000_baseline.sql) and derivePayoutBadge maps it to an 'Approved' badge, but PAYOUT_STATUS_FILTERS offers only all/queued/paid/failed/reversed - there is no way to filter the payouts ledger to approved rows, and matchesPayoutStatus would handle it if the option existed (the fallthrough p.status === f). Any legacy-path payout sitting in 'approved' is findable only by scrolling 'All statuses'.
    scenario: An org migrated from the legacy manual-approval flow has payouts stuck at 'approved' (approved but never transferred). The operator filters by Failed and Held, sees nothing, and concludes payouts are healthy; the approved-but-unpaid rows hide in the unfiltered list.
    evidence: 000_baseline.sql:127-135 payout_status enum includes 'approved'; derivePaymentsBadges.ts:29-30 maps 'approved'; payments-types.ts:12,22-28 PayoutStatusFilter/PAYOUT_STATUS_FILTERS omit it; derivePayments.ts:65-70 generic fallthrough would support it.
    verdict: Verified end-to-end. The payout_status enum includes 'approved' (000_baseline.sql:127-135; migration 092 documents the legacy lifecycle pending->approved->paid->bank_paid). derivePayoutBadge maps it to an 'Approved' badge (derivePaymentsBadges.ts:29-30, payments-presenters.tsx:32), but PayoutStatusFilter/PAYOUT_STATUS_FILTERS (payments-types.ts:12,22-28) omit it, and the matchesPayoutStatus fallthrough (derivePayments.ts:69) would support it triv
    correction: Minor sharpening only: the gap is slightly worse than 'no filter option' — the reconcile sweep (reconcile.ts:463) and the triage band (usePaymentsTriage.ts:102,109) both ignore 'approved', and the detail sheet offers no footer action for it (PaymentDetailSheet.tsx:122,135), so a legacy approved-but-

--- [LOW] payments/appointments realtime depends on dashboard-only publication config not reproduced in migrations  => CONFIRMED (high, severity=low)
    file: src/hooks/useAdminData.ts:797  category: visibility
    summary: The Payments ledger and KPI tiles rely on a postgres_changes subscription on the payments table ('charges land async from Stripe webhooks') and the triage band on appointments, but no migration adds payments or appointments to the supabase_realtime publication - migration 081 adds payouts/refunds/disputes and its own comment concedes 'the original core tables were added to the publication via the Supabase dashboard historically'. Any rebuilt or new environment (fresh Supabase project, disaster recovery, local db reset used for verification) silently loses ledger/triage liveness: webhook-driven charge results will not appear until a manual refetch or remount, with no error.
    scenario: Prod is migrated to a new Supabase project by replaying migrations. Everything passes; weeks later operators notice completed jobs' charges do not appear in Payments until they reload the tab. Nothing logs an error because a subscription to an unpublished table just never receives events.
    evidence: useAdminData.ts:794-806 payments sub with the comment about webhook-async charges; usePaymentsTriage.ts:163-180 appointments/payouts subs; 081_realtime_enable.sql:5-7,36-47 publication list lacks payments and appointments; grep of all migrations shows no 'ALTER PUBLICATION supabase_realtime ADD TABLE' for either.
    verdict: Verified every evidence claim by reading the code. useAdminData.ts:794-806 subscribes to payments (with the webhook-async comment); usePaymentsTriage.ts:163-180 subscribes to appointments/payouts. Grep of all active migrations confirms no ALTER PUBLICATION supabase_realtime ADD TABLE for payments or appointments; 081_realtime_enable.sql:5-7 explicitly admits core tables were added via the dashboard historically. The refutation attempt made it wor
    correction: Finding is accurate; two refinements. (1) The triage hook path is src/components/redesign/payments/usePaymentsTriage.ts (lines 163-180), not src/hooks/. (2) The mechanism is more concrete than 'dashboard-only config': a durable migration DID once exist — 056_realtime_publication_expansion.sql added 


================================================================================
## DOMAIN: payouts-connect

COVERAGE: Read in full: src/lib/stripe/transfers.ts; src/lib/stripe/charges/splits.ts; src/lib/stripe/connect/{cleaner,tenant,accountSlot}.ts; src/lib/payments/{settleCleanerPayout,isCleanerPayable,clawback,reconcile,dispatchStripeEvent}.ts; src/app/api/payouts/[payoutId]/{retry,dismiss}/route.ts; src/app/api/organizations/[orgId]/cleaner-payouts/route.ts; src/app/api/stripe/connect/{account-status,cleaner/start,create-account,reconcile-payouts}/route.ts; src/app/api/stripe/webhook/route.ts; src/app/api/cron/reconcile-payments/route.ts; src/app/api/platform/cleaners/[id]/connect/reset/route.ts; src/lib/auth/requireOrgAuth.ts; src/lib/monitoring/platformAlert.ts; src/lib/stripe.ts (Connect helpers 277-440); relevant parts of src/hooks/useCleanerData.ts; UI: CleanerEarnings.tsx, CleanerEarningsView.tsx, deriveEarnings.ts, CleanerPayoutsHistory.tsx, PayoutsSection.tsx, PayoutTimingNotice.tsx, usePaymentsTriage.ts; docs/stripe-architecture.md cutover section; migrations 067 (cron) and 088 (payouts uniqueness). SOLID: transfer creation is structurally platform->connected only (transfers.ts makes the forbidden connected->connected call impossible); the split math (splits.ts) conserves exactly (fee capped at tenant remainder, cleaner floored); settleCleanerPayout's carve/hold/repair state machine is careful (snapshot-based retry prevents percent-edit conservation breaks; a transfer-id-bearing retryable row is repaired rather than re-transferred, preventing double-pay; concurrent settlement is safe via the 088 unique index + shared idempotency keys); retry/dismiss route authz is correct (requireOrgAuth membership check + org-ownership check on the payout + manager can_manage_payments gate); the webhook route's claim-first idempotency and livemode guard are sound; the account-slot claim/commit/release flow with attempt-number-bumped idempotency keys is well designed; the platform cleaner-connect reset route has a proper in-flight-payout guard and audit logging; the admin "Needs you now" triage band gives owners/admins live, actionable visibility of failed and held payouts.

VISIBILITY: ADMIN: good on the happy-triage path, blind elsewhere. The Payments triage band (usePaymentsTriage.ts) surfaces failed payouts (Retry/Dismiss) and held payouts (grouped, "Message cleaner") with realtime refresh - genuinely actionable. But it is pull-only (no bell/notification fires for a failed or held payout), a dismissed failed payout disappears forever with no resurfacing, and a failed TENANT transfer (org itself not paid) has no surface at all: it exists only as a payment_events row that no UI in the app reads (verified: payment_events is referenced outside lib/payments only by the org-deletion route). recordPlatformAlert exists but is wired only to forgot-password, so the insufficient-platform-balance incident class that already happened in prod would again be silent. CLEANER: the embedded Stripe payouts component is truthful for money that reached Stripe/bank, but the app's own payout rows have no cleaner-facing surface in the live redesign UI - the Earnings "clearing" list covers only payments still processing (status='processing'), so a held slice (not onboarded) or a failed transfer is invisible to the cleaner (the legacy CleanerPayoutsHistory that showed these is unreachable post-cutover). PROD-SPECIFIC: because the live webhook is missing payout.paid/payout.failed/transfer.reversed and the only bank_paid backstop route (/api/stripe/connect/reconcile-payouts) has zero callers, payout rows can never reach bank_paid in prod - so both payout-domain notifications ('cleaner_paid' to the cleaner and 'clawback_blocked' to admins) are dead code today, and payout status in our DB permanently reads 'paid' regardless of what the bank did. HOMEOWNER: no payout-domain exposure needed; not applicable beyond refunds (out of this domain).

NOTIFICATION MATRIX:
Payout-domain events -> who is told, how, and whether it works in prod today: (1) cleaner transfer failed -> payment_events 'cleaner_transfer_failed' only; admin sees it ONLY by opening Payments (triage band); no bell/email; cleaner sees nothing. (2) cleaner slice held (not onboarded) -> payment_events 'cleaner_payout_held'; admin triage band only; cleaner sees only a generic "Set up payouts" card, not the held amount. (3) tenant transfer failed (org unpaid) -> payment_events 'tenant_transfer_failed'; NOBODY notified, NO UI surface. (4) payout landed in cleaner bank -> 'cleaner_paid' bell via handlePayoutPaid - DEAD in prod (payout.paid not delivered; reconcile-payouts route uncalled). (5) clawback blocked because funds already banked -> 'clawback_blocked' admin bell - DEAD in prod (bank_paid unreachable). (6) clawback/reversal failed -> payment_events 'cleaner_clawback_failed' (sweep retries) / 'refund_clawback_failed' / 'transfer_reversal_failed' (NO retry, NO surface). (7) dispute opened / dispute lost / refund failed -> admin bell notifications; these are platform-account events and do work. (8) platform balance insufficient (the real prod incident) -> nothing; recordPlatformAlert is wired only to forgot-password.

--- [HIGH] Missing prod webhook events make bank_paid unreachable: clawback's already-banked guard is inert and both payout notifications are dead  => UNVERIFIED (verifier never ran)
    file: src/lib/payments/clawback.ts:118  category: visibility
    summary: The only writers of payouts.status='bank_paid' are handlePayoutPaid (dispatchStripeEvent.ts:1007) and the /api/stripe/connect/reconcile-payouts route. Prod's live webhook does not subscribe to payout.paid/payout.failed/transfer.reversed (docs/stripe-architecture.md:151-152 lists adding them as a not-yet-done cutover step), and the reconcile-payouts route has ZERO callers (grep across src/ finds only the route file itself; the 15-min cron at api/cron/reconcile-payments/route.ts:53-62 runs no bank-payout job). So in prod no payout row can ever reach bank_paid. Blast radius: (a) clawbackCleanerPayout's guard at clawback.ts:118-150, which exists to block auto-reversal once funds left for the cleaner's bank and route it to an ops decision, can never trigger - a dispute-lost or ACH-return clawback days later will silently reverse a transfer whose money is already in the cleaner's bank, driving their connected balance negative (the exact outcome the code says needs an ops decision); (b) the 'cleaner_paid' notification (dispatchStripeEvent.ts:1072, 1081-1111) and 'clawback_blocked' admin notification (clawback.ts:140-147) - the only two payout-domain notifications - can never fire; (c) an out-of-band Dashboard transfer reversal is never mirrored (handleTransferReversed, dispatchStripeEvent.ts:946-967), so a reversed payout keeps reading 'paid' and settle's repair path treats the row as terminal.
    scenario: Cleaner completes a job Monday; transfer paid; Stripe daily payout lands in their bank Wednesday (row still 'paid' because payout.paid never arrives). Homeowner's ACH debit returns Friday -> payment_intent.payment_failed -> clawbackCleanerPayout sees status='paid' (not 'bank_paid'), skips the guard, reverses the transfer: the cleaner's connected balance goes negative with no ops review, no admin notification, and the DB shows 'reversed' while the cleaner keeps the bank money until Stripe recoups. Meanwhile no cleaner ever receives a 'you got paid' notification for any job.
    evidence: clawback.ts:118 `if (payout.status === 'bank_paid') {` (guard); dispatchStripeEvent.ts:1016-1018 `if (!connectedAccountId) { console.warn('payout.paid event missing event.account ... Will be caught by reconcile.'` (but no cron job reconciles); api/cron/reconcile-payments/route.ts:53-62 job list has no bank-payout reconciliation; grep for 'reconcile

--- [HIGH] Partial refund never shrinks a held or failed cleaner payout slice; later retry overpays the cleaner from platform funds  => UNVERIFIED (verifier never ran)
    file: src/lib/payments/settleCleanerPayout.ts:268  category: money-correctness
    summary: First settlement carves the cleaner slice into a payouts row ('pending' when not Connect-ready, 'failed' when the transfer errored) with no stripe_transfer_id. The refund unwind (reverseJobTransfersForRefund, clawback.ts:271-340) reverses only ACTUAL Stripe transfers, and loadCleanerPayout (clawback.ts:82-86) explicitly filters `.not('stripe_transfer_id','is',null)`, so a carved-but-untransferred slice is invisible to it. On retry, settleCleanerPayout deliberately pays the carved snapshot verbatim (line 268: `cleanerSettleCents = hasCarvedSlice ? Math.round(Number(priorPayout!.amount)*100) : cleanerCents`), ignoring the refund-shrunk split it just computed at lines 92-125. Only a FULL pre-settlement refund retires held/failed rows (lines 99-116). Net: after a partial refund, the tenant's transfer is proportionally reversed but the cleaner is later paid their full original slice, and the platform balance eats the cleaner's share of the refund.
    scenario: $100 job completes with a not-yet-onboarded 30% cleaner: tenant gets $69, $1 fee kept, $30 held as a 'pending' payout. Admin refunds $50 to the homeowner: reverseJobTransfersForRefund reverses $34.50 from the tenant transfer, the held row is untouched. Cleaner finishes onboarding next week; retryFailedPayouts pays them the full $30. Conservation: platform received $100, paid out $69 - $34.50 + $50 (refund) + $30 = $114.50 minus $1 fee kept -> platform is out $15 (the cleaner's share of the refund), silently. Same path triggers when the first transfer failed on insufficient platform balance (the real prod incident) and a partial refund lands before the sweep succeeds.
    evidence: settleCleanerPayout.ts:261-273 (carved slice overrides refund-adjusted cleanerCents; comment says 'A carved slice is paid even if the current share is now 0'); settleCleanerPayout.ts:125 `splitBaseCents = Math.max(0, capturedTotalCents - processingFeeCents - refundedCents)` is computed then discarded for carved slices; clawback.ts:82-86 `.not('stri

--- [HIGH] Failed transfer reversal during refund unwind is never retried and never surfaced - platform permanently eats the refund  => UNVERIFIED (verifier never ran)
    file: src/lib/payments/clawback.ts:331  category: money-correctness
    summary: reverseJobTransfersForRefund is best-effort: when reversePlatformTransfer throws for the tenant transfer it records payment_events 'transfer_reversal_failed', and for the cleaner transfer 'refund_clawback_failed' (clawback.ts:320-336), then returns without throwing. The webhook handler therefore completes and the event is marked processed (webhook route), so there is no dead-letter redelivery; the reconcile sweep's retryStrandedClawbacks selects ONLY event_type='cleaner_clawback_failed' (reconcile.ts:589-590), so neither of these two event types is ever retried. The in-code comment claims the refund path 'self-heals via the charge.refunded webhook re-running this' - but Stripe does not redeliver a processed event; it only re-runs if ANOTHER refund later lands on the same charge. No UI reads payment_events (verified), so the loss is invisible.
    scenario: Admin refunds a $200 settled job. The refund route pays the homeowner $200 from the platform balance, then reverseJobTransfersForRefund hits a transient Stripe 500/timeout reversing the tenant's $158 transfer. 'transfer_reversal_failed' is logged to payment_events and nothing else happens, ever: the tenant keeps $158 for a job the customer got refunded, the platform balance is out $158, no admin or platform-owner alert fires, and no sweep or UI will ever show or retry it.
    evidence: clawback.ts:331 `eventType: isCleanerTransfer ? 'refund_clawback_failed' : 'transfer_reversal_failed'` with comment 'record a DISTINCT type the full-clawback sweep ignores'; reconcile.ts:589 `.eq('event_type', 'cleaner_clawback_failed')` is the only stranded-clawback selector; api/stripe/webhook/route.ts:104-106 marks the event processed because ha

--- [HIGH] Failed tenant transfer (org not paid) is invisible: no payout row, no triage entry, no notification, no platform alert  => UNVERIFIED (verifier never ran)
    file: src/lib/payments/settleCleanerPayout.ts:188  category: visibility
    summary: When the tenant-remainder transfer fails, settleCleanerPayout records payment_events 'tenant_transfer_failed' and bails (lines 188-198) - correctly not paying the cleaner first - but this state has no representation anywhere an operator can see: no payouts row is created (the admin triage band at usePaymentsTriage.ts:98-111 queries only the payouts table and appointment authorization_status), no notification_event is recorded (settle emits zero notifications), and recordPlatformAlert is wired only to forgot-password (grep: src/app/api/auth/forgot-password/route.ts is its sole caller). The settleUnsettledCaptures sweep (reconcile.ts:373-438) does silently retry every 15 minutes, but when the cause is a persistently negative/insufficient platform balance - which actually happened in prod when platform_fee_bps was 0 - every retry fails silently forever.
    scenario: Platform balance goes negative again (fee misconfig, refund spike, Stripe fee drift). Every completed job's homeowner charge captures fine (payments row reads 'paid'), but the tenant transfer fails on every settle and every 15-minute sweep retry. The org's revenue accumulates on the platform balance for days; the admin Payments screen shows nothing wrong (no failed payout rows exist because settle bails before the cleaner leg), cleaners' cuts also never move, and the only trace is payment_events rows nobody can see. Discovery happens when the org owner notices their Stripe embed shows no incoming payouts.
    evidence: settleCleanerPayout.ts:188-198 (records 'tenant_transfer_failed', returns; no payout row, no notification); usePaymentsTriage.ts:98-111 (triage queries payouts status='failed'/'pending' only - a tenant-leg failure creates neither); src/lib/notifications/eventTypes.ts has no tenant-transfer/payout-failed type; recordPlatformAlert callers: only forgo

--- [MEDIUM] Fixed transfer idempotency keys make every payout retry within 24h of a failure guaranteed to fail (admin Retry button included)  => UNVERIFIED (verifier never ran)
    file: src/lib/payments/settleCleanerPayout.ts:322  category: idempotency
    summary: Both transfer legs use fixed idempotency keys - `tenant-payout-${appointmentId}` (line 177) and `cleaner-payout-${appointmentId}` (line 322) - with no attempt counter. Stripe caches the response of an executed request against its key for ~24h, including 4xx failures like balance_insufficient, and rejects same-key-different-params; the codebase itself documents this exact behavior for charges and bumps `charge-{id}-{attempt}` / reauth_count to escape it (dispatchStripeEvent.ts:589-592: 'the spent idempotency key ... is cached against the old decline, so without a fresh attempt number the retry would replay the cached failure') and bumps stripe_connect_attempt_number for account creation - but transfers never got the same treatment. So after a failed transfer, the admin 'Retry payout' route (which promises 'Force an immediate re-settlement', retry/route.ts:28-33) and all ~96 fifteen-minute sweep retries inside the 24h window are doomed: Stripe replays the cached failure (or errors on param mismatch, since retries pass sourceTransactionId=null vs the original charge id). The payout only truly heals once the key ages out.
    scenario: Platform balance dips; a cleaner transfer fails with balance_insufficient at 9am; payout row goes 'failed' and appears in the admin triage band. Bridger tops up the balance at 10am and clicks Retry: Stripe replays the cached balance_insufficient, the route returns the misleading copy 'Make sure their Stripe payout setup is complete, then retry'. Every retry and sweep pass fails identically until ~9am the next day. During the real prod negative-balance incident this adds a hard 24h floor to cleaner-payment recovery after the underlying fix.
    evidence: settleCleanerPayout.ts:322 `idempotencyKey: \`cleaner-payout-${appointmentId}\`` and :177 tenant equivalent (no attempt component); transfers.ts:51 passes the key straight to stripe.transfers.create; retry/route.ts:98 passes platformChargeId=null so retry params differ from the original (source_transaction omitted); contrast dispatchStripeEvent.ts:

--- [MEDIUM] Cleaner has no view of held or failed payout rows in the live redesign UI, and admin dismiss hides a failed payout forever  => UNVERIFIED (verifier never ran)
    file: src/components/redesign/cleaner/earnings/CleanerEarningsView.tsx:88  category: ui
    summary: The redesign Earnings screen shows only (a) the Stripe embed (truthful for money already at Stripe/bank) and (b) a 'clearing' list built exclusively from payments with status='processing' (useCleanerData.ts:477 `.eq('status','processing')`). A payouts row that is 'pending' (slice held because the cleaner isn't onboarded) or 'failed' (transfer errored) appears in NEITHER: the payment is already 'paid' so it leaves the clearing list, and the held/failed amount has no other cleaner-facing surface (legacy CleanerPayoutsHistory, which showed these states, lives only on the legacy cleaner-dashboard page, unreachable after cutover). The not-onboarded setup card even keys its 'money waiting below' copy on the clearing list (hasWaiting), so a cleaner with a held $200 slice but no in-flight ACH sees 'No earnings yet'. On the admin side, dismissing a failed payout sets attention_dismissed_at (dismiss/route.ts:70-73) and the triage filter `.is('attention_dismissed_at', null)` (usePaymentsTriage.ts:103) removes it permanently - there is no undismiss and nothing re-flags the row when subsequent sweep retries keep failing, so a permanently-failing payout (e.g. restricted cleaner account) becomes invisible to everyone while remaining unpaid.
    scenario: Cleaner completes 3 jobs before finishing Stripe onboarding; $340 is held as 'pending' payout rows. Their Earnings tab shows the setup card and 'No earnings yet' - no amounts - so they don't realize completing onboarding releases real money (the exact Wanda-Jones-style onboarding stall). Separately: a cleaner's account gets restricted, their payout fails, the admin clicks Dismiss intending 'seen it'; the sweep keeps failing silently forever and no human ever sees that payout again.
    evidence: CleanerEarningsView.tsx:88-96 (only clearing rows or 'No earnings yet'); useCleanerData.ts:477 `.eq('status','processing')` (held/failed payouts excluded); deriveEarnings.ts:38-47 (no payout-row input at all); dismiss/route.ts:70-73 + usePaymentsTriage.ts:103 (`attention_dismissed_at` one-way); CleanerPayoutsHistory.tsx mounted only via legacy src/

--- [LOW] Earnings mode derived from the org-wide default payout model, not the cleaner's own payout_model  => UNVERIFIED (verifier never ran)
    file: src/components/redesign/cleaner/earnings/CleanerEarnings.tsx:47  category: ui
    summary: deriveEarnings decides between 'employee' mode (no payout UI at all: 'Your office handles your pay') and 'connect' mode using `currentOrganization?.default_payout_model` (CleanerEarnings.tsx:47), while settlement and payability decisions use the per-cleaner `cleaner_profiles.payout_model` (settleCleanerPayout.ts:151, isCleanerPayable.ts:33). A percentage cleaner inside an org whose default is hourly_external would see the employee screen, never get the Connect setup card, never onboard - and their carved slices would sit held as 'pending' payouts indefinitely. Currently mitigated because the org-profile route constrains default_payout_model to 'percentage_contractor' (organizations/[orgId]/profile/route.ts:9), but the divergence is a loaded trap for when hourly ships.
    scenario: Org flips default_payout_model to hourly_external once that model launches, but keeps two legacy percentage cleaners. Those cleaners' Earnings tab switches to 'Your office handles your pay'; their completed jobs keep carving held payout slices (settle uses their per-cleaner payout_model/percent) that they can never release because the onboarding UI is unreachable for them.
    evidence: CleanerEarnings.tsx:47 `payoutModel: currentOrganization?.default_payout_model ?? 'percentage_contractor'`; deriveEarnings.ts:34 `if (payoutModel === 'hourly_external') mode = 'employee'`; settleCleanerPayout.ts:151 gates on the CLEANER row's `cleaner.payout_model !== 'hourly_external'`.

--- [LOW] Orphaned legacy Connect routes: create-account bypasses the race-safe slot claim and reports success on a failed DB write  => UNVERIFIED (verifier never ran)
    file: src/app/api/stripe/connect/create-account/route.ts:61  category: correctness
    summary: /api/stripe/connect/create-account has no UI caller (grep: only useCleanerConnect -> cleaner/start, useStripeConnect -> account-status/login-link are used) but remains a live, cleaner-self-authorized endpoint that predates the incident-2026-05-28 hardening: it calls createConnectAccount with NO idempotency key and NO slot claim (route.ts:61), so concurrent/retried calls mint orphan Stripe accounts; and when the follow-up DB write of stripe_connect_account_id fails it only console.errors and still returns `{ success: true, account_id }` (route.ts:63-72), leaving an untracked account - the drift class the accountSlot machinery was built to prevent. Same staleness applies to the uncalled onboarding-link route. These should be retired or folded into /cleaner/start.
    scenario: A cleaner (or a stale client build) POSTs create-account twice in quick succession, or once while cleaner/start is mid-claim: two Stripe Express accounts exist for one cleaner; if the DB update also fails, the app believes account creation succeeded while cleaner_profiles.stripe_connect_account_id stays null, and the next /start creates a third account - transfers and account.updated drift detection then disagree about which account is real.
    evidence: create-account/route.ts:61 `const account = await createConnectAccount(email, name);` (no options/idempotencyKey, no claimConnectAccountSlot); :68-70 `if (updateError) { console.error(...) }` then :72 `return NextResponse.json({ success: true, account_id: account.id })`; contrast cleaner/start/route.ts:60-91 + accountSlot.ts claim/commit/release.


================================================================================
## DOMAIN: refunds-cancel

COVERAGE: Read in full: src/lib/payments/refundCancelledCharge.ts, refundGuards.ts, cancellationFee.ts, chargeCancellationFee.ts, clawback.ts, settleCleanerPayout.ts, reconcile.ts (dead-letter retry + stranded-clawback sweep), dispatchStripeEvent.ts (handleChargeRefunded, handleRefundStatusChange, handleChargeFailed, settlement guards); src/lib/stripe/charges/refund.ts, splits.ts; src/app/api/payments/[paymentId]/refund/route.ts; src/app/api/appointments/[appointmentId]/cancel/route.ts; src/lib/auth/requireOrgAuth.ts; UI: RefundModal.tsx, CancelWithFeeModal.tsx, CancelBookingDialog.tsx, CancelCleaningSheet.tsx, redesign OperatorPayments.tsx + PaymentDetailSheet.tsx, homeowner receipts derive-payments.ts, notifications eventTypes/labels/deriveNotifications; migrations 065 (refunds DDL), settings routes (payment-settings), useCancelMyCleaning, useAdminData PAYMENTS_INFINITE_SELECT; docs/stripe-architecture.md decisions #7/#10. SOLID: refund-route authz (owner/admin only, org-scoped paymentId with payment.organization_id check against a membership-verified org id); the refund-before-reverse ordering; the cumulative-target idempotency key (refund-{apptId}-cum-{N}) which correctly dedupes double-submits while allowing repeated equal partials; refunds.stripe_refund_id UNIQUE (065:217) kills duplicate-ledger races; Stripe itself hard-caps cumulative refunds at the charge amount so concurrent refunds cannot over-refund the payer; the settle-time refund guard (refundGuards + settleCleanerPayout:92-125) closes the refund-before-settlement window including the DB fallback when Stripe is unreadable; refundCancelledInflightCharge's attempt-counter keying for failed-refund retries; handleRefundStatusChange correctly reverts 'refunded' to 'paid' on a failed refund and alerts admins; cancellation fee math is capped at gross, homeowner callers are forced party=homeowner/no_show=false and own-appointment scoped; platform-fee treatment on refund is conservation-correct (platform retains fee at charge, absorbs the proportional fee on refund, netting to zero; only Stripe's own processing fee is eaten, a business cost not a bug).

VISIBILITY: Split verdict. FULL refunds are visible: the payments row flips to 'refunded' and both the operator transactions list and the homeowner receipts render a 'refunded' badge, and admin bell notifications exist for refund_failed, cancelled_job_refunded, cancellation_fee_failed, and clawback_blocked. Everything else in this domain is invisible: no UI anywhere reads the refunds table, so PARTIAL refunds (payment stays 'paid' at the original amount) and per-refund state are undiscoverable by operator and homeowner alike; out-of-band Stripe Dashboard refunds generate no notification; a failed transfer reversal after a refund (the platform-loses-money state) is recorded only in the payment_events forensic ledger with no notification, no sweep, and no UI; the homeowner is never notified of any refund (no homeowner-audience refund event type exists); the operator payments query selects no charge_kind, so a cancellation-fee payment is indistinguishable from a job payment; and the cancel dialog tells the operator "The customer and cleaner will be notified" when the cancel route notifies neither.

NOTIFICATION MATRIX:
Event -> admin notified / homeowner notified / cleaner notified / visible in UI: Full refund (in-app): no (actor-initiated) / NO / no / yes (refunded badge, both sides). Partial refund: no / NO / no / NO (payment stays 'paid' at original amount; nothing reads refunds table). Out-of-band Dashboard refund: NO / NO / no / only if full (status flip via webhook). Refund failed/canceled at Stripe: YES (refund_failed bell, dispatchStripeEvent.ts:530-537) / NO / no / status reverts to paid. Transfer-reversal failure after refund (platform out the money): NO (payment_events only) / no / no / NO. Clawback blocked (bank_paid, dispute/ACH path): YES (clawback_blocked) / n-a / no / ledger only. Cleaner clawed back via refund path: no / no / NO / payout flips to 'reversed' in payouts ledger. Cancellation (any party): NO / NO / NO (despite dialog copy) / appointment status only. Cancellation fee charged: no / NO (sheet shows it at cancel time only; admin-declared no-show homeowner never told) / no / unlabeled 'paid' payment row. Cancellation fee failed/uncollectable: YES (cancellation_fee_failed) / no / no / failed payment row. Auto-refund of cancelled in-flight debit: YES (cancelled_job_refunded, admin-audience) / NO / no / refunded on confirm.

--- [CRITICAL] Failed transfer reversals during a refund unwind have no retry and no alert; platform permanently out the money  => CONFIRMED (high, severity=critical)
    file: src/lib/payments/clawback.ts:331  category: money-correctness
    summary: When reverseJobTransfersForRefund's Stripe reversal call fails, it records event types ('refund_clawback_failed' for the cleaner leg, 'transfer_reversal_failed' for the tenant leg) that the reconcile sweep never retries (retryStrandedClawbacks filters only 'cleaner_clawback_failed', reconcile.ts:589), emits no notification, and the refund route ignores the failures count and returns success:true (refund/route.ts:155-182). The claimed self-heal, the charge.refunded webhook re-running the unwind, is a single shot: the handler never throws, so the webhook_events row is marked processed and the dead-letter retry (reconcile.ts:58-79, .neq('status','processed')) never replays it. For an out-of-band Dashboard refund the webhook attempt is the ONLY unwind.
    scenario: Admin refunds a $200 paid job whose transfers already went out. The homeowner is refunded $200 from the PLATFORM balance (route step 1 succeeds), then reversePlatformTransfer fails (cleaner's connected account briefly restricted, or Stripe transient error). Route returns success. The one charge.refunded delivery re-runs the unwind and fails again (same restriction), records refund_clawback_failed, and the event is marked processed. Terminal state: homeowner refunded, tenant and cleaner keep ~$198, platform is out ~$198 forever. No sweep retries it, no admin bell fires (contrast: clawback_blocked and refund_failed both notify), the only trace is a payment_events row no UI displays.
    evidence: clawback.ts:320-336: catch block records eventType: isCleanerTransfer ? 'refund_clawback_failed' : 'transfer_reversal_failed' and continues (never throws). reconcile.ts:586-593: sweep queries .eq('event_type', 'cleaner_clawback_failed') only; the comment at clawback.ts:327-331 says the full-clawback sweep must ignore these types, but nothing else p
    verdict: Every cited claim survives adversarial verification. (1) clawback.ts:320-336: a failed Stripe reversal in reverseJobTransfersForRefund records eventType 'refund_clawback_failed' (cleaner leg) or 'transfer_reversal_failed' (tenant leg) and never throws; the in-code comment (327-330) deliberately excludes these types from the full-clawback sweep to avoid over-clawback on partial refunds, but nothing else consumes them. (2) Repo-wide grep: these two

--- [HIGH] The configured no-show fee (no_show_fee_type/value) is never charged; no-shows bill the late-cancellation fee instead  => CONFIRMED (high, severity=high)
    file: src/app/api/appointments/[appointmentId]/cancel/route.ts:127  category: money-correctness
    summary: Both settings UIs and the payment-settings API let an org configure a distinct no-show fee ('Charged when the customer is not home at the scheduled time', CancellationSection.tsx:108), and the route validates and persists no_show_fee_type/no_show_fee_value (payment-settings/route.ts:98-106). But the cancel route selects only cancellation_window_hours, cancellation_fee_type, cancellation_fee_value (cancel/route.ts:127) and computeCancellationFee takes a single feeType/feeValue applied to BOTH no-show and late-cancel (cancellationFee.ts:31-42). No money-path code reads no_show_fee_* at all (repo-wide grep: only settings UI + settings route).
    scenario: Org sets late-cancellation fee = none and no-show fee = $50 flat (a common policy: free cancels, but no-shows pay). Cleaner arrives, homeowner absent; admin cancels with no_show=true. computeCancellationFee gets feeType='none' so feeCents=0: the $50 the settings screen promised is never charged and no failure event fires (it is a 'legitimate' $0). With late-cancel $25 / no-show $50 configured, every no-show silently undercharges by $25. The dialog preview uses the same wrong inputs, so operator and homeowner both see the wrong-but-consistent number and nobody notices.
    evidence: cancel/route.ts:125-129: .select('cancellation_window_hours, cancellation_fee_type, cancellation_fee_value'); cancellationFee.ts:31-42: homeownerFault = party==='homeowner' && (noShow || insideWindow) then one feeType/feeValue branch. CancellationSection.tsx:35+108-135 and OrgPaymentSettings.tsx:59 read/write no_show_fee_type/no_show_fee_value; pay
    verdict: Verified in full. cancel/route.ts:125-129 selects only cancellation_window_hours/cancellation_fee_type/cancellation_fee_value; computeCancellationFee (src/lib/payments/cancellationFee.ts:31-42) folds no-show and late-cancel into one homeownerFault branch using that single feeType/feeValue. Repo-wide grep confirms no_show_fee_type/no_show_fee_value exist only in migration 071, the payment-settings route (98-106), and the two settings UIs (OrgPayme
    correction: The auditor's claims are all accurate. One framing nuance: docs/stripe-architecture.md decision #10 specifies a SINGLE configurable fee for both late-cancel and no-show, so computeCancellationFee matches the original design; the defect was introduced when migration 071 and the settings surfaces adde

--- [HIGH] Refund unwind reverses the cleaner's transfer even after the payout is bank_paid, bypassing the deliberate clawback_blocked policy  => CONFIRMED (high, severity=high)
    file: src/lib/payments/clawback.ts:296  category: money-correctness
    summary: clawbackCleanerPayout deliberately refuses to reverse a payout whose status is 'bank_paid' because 'reversing the transfer would drive the connected balance negative with no predictable recovery path', and instead records clawback_blocked_bank_paid plus an admin clawback_blocked notification (clawback.ts:113-150). reverseJobTransfersForRefund, used by the in-app refund route AND the charge.refunded webhook, has no such guard: it loads the payout only to mirror status (clawback.ts:292, 305) and reverses every transfer in the group regardless, then overwrites payouts.status from 'bank_paid' to 'reversed' (clawback.ts:313-318), erasing the fact the funds already left Stripe.
    scenario: Cleaner is paid $80 Tuesday; with the org's daily payout schedule it reaches their bank Wednesday (payout status 'bank_paid'). Thursday the homeowner complains and the admin taps Refund. The cleaner's transfer is silently reversed: their connected balance goes ~-$80 (their next jobs' pay is swallowed with no warning, no clawback_blocked notification, no ops decision), or, if Stripe rejects the reversal, it lands in refund_clawback_failed which nothing retries or surfaces (finding 1). Because prod payouts are daily, essentially EVERY refund issued more than a day or two after completion hits this path.
    evidence: clawback.ts:113-118 (comment: 'Once the funds have LEFT Stripe for the cleaner's bank, reversing the transfer would drive the connected balance negative with no predictable recovery path. Surface it for an ops decision... instead of auto-reversing.') and 119-150 (the bank_paid block + notification) versus reverseJobTransfersForRefund clawback.ts:27
    verdict: Confirmed by reading the code. clawbackCleanerPayout (src/lib/payments/clawback.ts:114-150) deliberately blocks reversal when payouts.status='bank_paid' (records clawback_blocked_bank_paid + admin clawback_blocked notification), while reverseJobTransfersForRefund (clawback.ts:271-340) loads the same payout row (line 292) but never checks its status before reversePlatformTransfer (line 307), and on a full refund overwrites status to 'reversed' fro
    correction: Two accuracy nuances: (1) "erasing the fact the funds already left Stripe" is overstated — the full-refund path overwrites payouts.status bank_paid→'reversed' but does not null bank_paid_at, so the timestamp survives (the status field ops flows key on is still lost, and a partial refund keeps the ol

--- [HIGH] Partial and out-of-band refunds are invisible in every UI and generate no notification  => CONFIRMED (high, severity=high)
    file: src/hooks/useAdminData.ts:688  category: visibility
    summary: No UI in the app reads the refunds table (repo-wide grep: zero from('refunds') outside lib/api), and the operator payments query PAYMENTS_INFINITE_SELECT (useAdminData.ts:688-701) selects neither refund data nor charge_kind. A partially refunded payment keeps status 'paid' and displays its ORIGINAL amount in the operator transactions list, the PaymentDetailSheet, and homeowner receipts; the refunded portion exists only in the refunds table and Stripe. An out-of-band Stripe Dashboard refund additionally notifies nobody: handleChargeRefunded records only a payment_events row (dispatchStripeEvent.ts:719-729), no notification_events.
    scenario: Support issues a $75 partial refund of a $200 job in the Stripe Dashboard (the only partial path since the redesign UI is full-refund-only). The webhook claws back $75-proportional transfers correctly, but the operator payments screen still shows '$200 / Paid', the detail sheet still offers a 'Refund' button whose toast will say 'Payment refunded', and the homeowner's receipt still reads $200 paid. No admin is told the Dashboard refund happened. The org's books, the homeowner's expectation, and the app UI all disagree with Stripe.
    evidence: useAdminData.ts:688-701: PAYMENTS_INFINITE_SELECT = id, amount, status, payment_type, payment_method, reference, notes, paid_at, created_at, is_self_pay, appointment(...) with no refunds join and no charge_kind. derive-payments.ts:16-21: homeowner badge derives from payments.status alone. dispatchStripeEvent.ts:697-701: payment flips to 'refunded' 
    verdict: Verified against the code. (1) No UI reads the refunds table: from('refunds') exists only in api routes and lib/payments; the sole UI touchpoint is a realtime invalidation sub on refunds (useAdminData.ts:774-783) whose refetched select (PAYMENTS_INFINITE_SELECT, useAdminData.ts:688-709) carries no refund fields, so it is inert. (2) A partial refund leaves payments.status='paid' (refund route flips only at full coverage, refund/route.ts:144-147; w
    correction: Two details are mis-stated. (a) After a Stripe-Dashboard partial refund, clicking the still-visible Refund button does NOT produce a "Payment refunded" success toast: handleChargeRefunded never inserts a refunds row for out-of-band refunds (it only updates existing pending rows by stripe_refund_id, 

--- [MEDIUM] The homeowner is never notified of any refund, including the auto-refund of a debit for a job they cancelled  => CONFIRMED (high, severity=medium)
    file: src/lib/notifications/eventTypes.ts:32  category: notification
    summary: The notification catalog contains no homeowner-audience refund event: the refund route emits no notification at all, handleChargeRefunded emits none, and cancelled_job_refunded (the ACH-debit auto-refund) is explicitly admin-audience (refundCancelledCharge.ts:119-126, payload audience:'admin', catalog comment 'recipient: admins'). The only homeowner-visible signal is the receipts badge flipping to 'refunded', which happens only on a FULL refund and only if the homeowner goes looking.
    scenario: A homeowner's bank account was debited $180 for a job that got cancelled while the ACH was in flight. The system correctly auto-refunds it, and admins get a bell notification, but the homeowner, the person whose money was taken for a cancelled job, receives nothing and sees a bank debit with no explanation for the several days the refund takes to land. Support call guaranteed. Same for admin-issued refunds: the customer learns about their refund from their card statement, not the product.
    evidence: eventTypes.ts:12-37: full catalog; the only refund-related types are cancelled_job_refunded ('recipient: admins') and refund_failed ('recipient: admins'). refundCancelledCharge.ts:119-126: recordNotificationEvent with payload { audience: 'admin' } only. refund/route.ts contains zero recordNotificationEvent calls; dispatchStripeEvent.ts handleCharge
    verdict: Confirmed on every cited point. eventTypes.ts:12-37 contains no homeowner-audience refund event (cancelled_job_refunded and refund_failed are both commented 'recipient: admins'). The admin refund route (src/app/api/payments/[paymentId]/refund/route.ts, read in full) makes zero recordNotificationEvent or email calls. handleChargeRefunded (dispatchStripeEvent.ts:681-730) emits only a forensic payment_events row. refundCancelledCharge.ts:119-126 emi
    correction: The finding is accurate except the headline failure scenario: the multi-day in-flight ACH-debit example is unreachable in prod today because STRIPE_ACH_ENABLED is off (src/lib/stripe/flags.ts:109). The live exposure is card refunds: admin-issued refunds via the live refund route and the card complet

--- [MEDIUM] Live operator refund is a single un-confirmed tap that issues an irreversible full refund; no partial option exists  => CONFIRMED (high, severity=medium)
    file: src/components/redesign/payments/PaymentDetailSheet.tsx:95  category: ui
    summary: In the redesign (the only operator UI reachable in prod; legacy 307s), the 'Refund' button in the payment detail sheet calls onRefund(txn.id) directly with no confirmation dialog, no amount entry, and no summary of consequences; handleRefund POSTs with no amount field, which the route treats as a FULL refund that also reverses the tenant and cleaner transfers (and per finding 3 can drive a bank_paid cleaner negative). The legacy RefundModal, which had a full/partial choice and an explicit confirm step, is unreachable, so partial refunds cannot be issued from the product at all.
    scenario: An admin opens a $450 payment's detail sheet to check the date and fat-fingers the Refund button. With zero confirmation, the full $450 is refunded to the homeowner, both transfers are reversed, and the cleaner's pay is clawed back. There is no undo; recovering requires re-charging the customer out-of-band. Separately, a customer asking for a $50 goodwill partial refund cannot be served from the app; the operator must use the Stripe Dashboard, landing in the invisible-partial-refund state of finding 4.
    evidence: PaymentDetailSheet.tsx:88-99: {txn.refundable ? <Button ... onClick={() => onRefund(txn.id)}>Refund</Button> : null}, no dialog. OperatorPayments.tsx:258-280: handleRefund body JSON.stringify({ organization_id: currentOrganizationId }) with no amount; refund/route.ts:83-84 treats missing amount as full (grossCents - alreadyRefunded). RefundModal.ts
    verdict: Verified end-to-end. PaymentDetailSheet.tsx:88-99 renders the Refund button gated only on txn.refundable, with onClick calling onRefund(txn.id) directly; no confirmation dialog exists anywhere in the sheet or parent. OperatorPayments.tsx:258-280 handleRefund immediately POSTs to /api/payments/[id]/refund with a body containing only organization_id (no amount). refund/route.ts:83-84 treats a non-numeric amount as a full refund (grossCents - alread

--- [MEDIUM] Cancel dialog promises 'The customer and cleaner will be notified' but the cancel route notifies neither, including when a fee is charged  => CONFIRMED (high, severity=medium)
    file: src/components/redesign/bookings/cancel/CancelBookingDialog.tsx:141  category: notification
    summary: CancelBookingDialog's description states 'The customer and cleaner will be notified', but POST /api/appointments/[appointmentId]/cancel contains zero recordNotificationEvent calls, and the notification catalog (eventTypes.ts:12-37) has no cancellation event type for homeowner or cleaner at all. The only notification the cancel flow can emit is the admin-audience cancellation_fee_failed inside chargeCancellationFee. A homeowner charged a no-show fee off-session (admin-declared, up to the full job price) is never told their card was charged, and the assigned cleaner is never told the job is gone.
    scenario: Admin cancels tomorrow's job as a homeowner no-show; the route charges the homeowner's saved card $60 off-session. The homeowner gets no notification of the charge and later disputes an unrecognized $60 (a chargeback the tenant pays $15 for). Meanwhile the cleaner, never notified, drives to the cancelled job the next morning. The operator, reassured by the dialog copy, believes both were informed.
    evidence: CancelBookingDialog.tsx:141: 'The customer and cleaner will be notified.' cancel/route.ts:1-234: imports and calls recordPaymentEvent only (forensic ledger); no recordNotificationEvent anywhere in the file. eventTypes.ts:12-37: no appointment-cancelled or fee-charged event type exists for any audience (chargeCancellationFee.ts:65-74 emits only admi
    verdict: Verified against the code. CancelBookingDialog.tsx:141 promises "The customer and cleaner will be notified", but POST /api/appointments/[appointmentId]/cancel (route.ts:1-234) imports/calls only recordPaymentEvent (append-only forensic ledger, not a notification), and a repo-wide search of recordNotificationEvent call sites confirms the cancel route is not among them. The notification catalog (src/lib/notifications/eventTypes.ts:12-37) contains n

--- [LOW] handleChargeRefunded relies on charge.refunds, which the pinned Stripe API version no longer embeds; refunds ledger rows can stay 'pending' forever  => CONFIRMED (high, severity=low)
    file: src/lib/payments/dispatchStripeEvent.ts:686  category: ledger-accuracy
    summary: handleChargeRefunded iterates charge.refunds?.data ?? [] to flip refunds rows from 'pending' to 'succeeded'. The SDK is pinned to apiVersion '2025-12-15.clover' (src/lib/stripe.ts:23); the Charge object stopped embedding the refunds list by default in Stripe API 2022-11-15, so on a webhook endpoint configured with a modern version this list is absent and the loop is a no-op. refund.updated only fires on post-creation changes, and card refunds are typically created already-succeeded, so nothing ever confirms the row. Money math is unaffected today because the route's cap counts 'pending' and 'succeeded' identically (refund/route.ts:73-77), but the ledger permanently misstates refund state and any future logic or UI keyed on 'succeeded' is silently broken.
    scenario: Admin refunds a card payment; Stripe creates the refund with status 'succeeded' immediately. The charge.refunded event arrives without an embedded refunds list, so the refunds row stays 'pending' indefinitely (no refund.updated ever fires for it). The payment itself still flips to 'refunded' via charge.amount_refunded, masking the drift until someone builds on refunds.status.
    evidence: dispatchStripeEvent.ts:686-693: const refundList = charge.refunds?.data ?? []; for (const r of refundList) update refunds set status='succeeded' where stripe_refund_id and status='pending'. src/lib/stripe.ts:23: apiVersion: '2025-12-15.clover'. handleRefundStatusChange (470-483) maps refund.updated/refund.failed but Stripe emits refund.updated only
    verdict: CONFIRMED as stated. (1) handleChargeRefunded (src/lib/payments/dispatchStripeEvent.ts:686-693) iterates charge.refunds?.data ?? [] on the charge taken directly from event.data.object (line 46) with no expand/re-retrieve; the SDK pin is '2025-12-15.clover' (src/lib/stripe.ts:23) and Stripe stopped embedding Charge.refunds by default at API 2022-11-15, so on any modern webhook endpoint version the loop is a no-op. (2) Both insert paths hardcode st


================================================================================
## DOMAIN: selfpay-ach

COVERAGE: Read in full: src/lib/payments/{chargeSelfPayAchAppointment,chargeAchAppointment,selfPayMath,settleSelfPay,clearFailedForBank,orgBilling,orgHomeowner,chargeCompletedAppointment,settleCleanerPayout,dispatchStripeEvent,reconcile,processingFee,paymentSectionState}.ts; src/lib/stripe/charges/{chargeSelfPay,chargeSelfPayAch,chargeAch,splits}.ts; src/lib/stripe/flags.ts; src/lib/stripe.ts (getOrCreateOrgSelfPayCustomer, createSetupIntent); src/app/api/stripe/org/{create-setup-intent,confirm-setup-intent,saved-payment-methods}/route.ts; src/app/api/organizations/[orgId]/payment-settings/route.ts; src/app/api/appointments/[appointmentId]/charge/route.ts and cancel/route.ts (relevant sections); src/app/api/payouts/[payoutId]/retry/route.ts; UI surfaces src/components/redesign/payments/usePaymentsTriage.ts, redesign/bookings/payment/OperatorPaymentSection.tsx, redesign/bookings/new-booking/{OperatorBookingForm,buildBookingInsert,deriveOperatorBooking}; src/lib/auth/requireOrgPaymentsAuth.ts; homeowner confirm-setup-intent for comparison. SOLID: selfPayMath fee-on-top math (cut floored on gross, uncapped 1% riding on top, ceil gross-up so cleaner+fee are never shorted, zero-cut zeroes everything); processingFee ACH gross-up incl. the $5 cap boundary; ACH pending discipline (no cleaner/tenant transfers until payment_intent.succeeded; 6-day reconcile SLA for processing rows; cancel-during-processing auto-refunds via refundCancelledInflightCharge; late ACH return after settlement claws back the cleaner and notifies); charge-claim RPC + 23505 upsert races; org payment-method routes' flag gating (404 when STRIPE_SELF_PAY_ENABLED off) and owner/admin/can_manage_payments authz; settleSelfPay refund guards (H2). The defects cluster in ACH failure recovery, self-pay payout retry routing, and self-pay/ACH failure visibility.

VISIBILITY: Poor once ACH or self-pay money leaves the happy path. Happy path is fine: homeowner ACH shows a Processing pill (payments.status='processing' -> paymentSectionState 'processing'), self-pay card declines stamp appointments.authorization_status='failed' and appear in the Payments triage band, and self-pay no-method/failure states emit admin notifications. But every failure surface keys on appointments.authorization_status, which the ACH paths never set: an ACH debit that bounces days later (payments.status='failed') appears in NO triage band, produces NO notification for homeowner-billed jobs, and the booking sheet maps it to 'before_charge' whose completed-job hint says "will be charged on the next billing run" (false, per finding 1 it never will). Self-pay ACH bounces do notify admins, but the only retry affordances either falsely report "Processing" (charge retry) or misroute money (payout retry). The booking sheet's payment section short-circuits every self-pay row to a bare "Self-pay" badge, hiding failed/processing/paid states and all recovery actions. Stranded self-pay settlements (cleaner unpayable at settlement) have zero UI surface at all: no payout row, no triage entry, only a payment_events row.

--- [CRITICAL] Failed ACH debit can never be retried: idempotency guard treats a failed row's PaymentIntent as 'already charging'  => UNVERIFIED (verifier never ran)
    file: src/lib/payments/chargeAchAppointment.ts:74  category: money-correctness
    summary: Both ACH orchestrations' pre-charge idempotency check matches ANY revenue row with a non-null stripe_payment_intent_id, with no status filter, so a row whose debit already FAILED (status='failed', PI id still on the row) short-circuits every subsequent attempt with ok:true code:'processing' ("Already charging") without creating a new debit. The reconcile sweep re-selects the appointment forever (completed, authorization_status NULL, payment not paid/processing), gets the false success, and even logs 'drift_repaired' each cycle; a manual operator Retry shows a Processing badge. The setup_intent.succeeded self-heal never fires (it keys on authorization_status, which ACH paths never set) and clearFailedForBank no-ops for the same reason, so the failed row's PI is never cleared. The money is silently never collected unless an operator happens to swap the payment method to a card.
    scenario: ACH flag on. Homeowner's $300 job completes; chargeAchAppointment creates the debit (row status='processing', PI saved). Four days later the debit is returned (insufficient funds); payment_intent.payment_failed marks the row status='failed'. Every subsequent chargeUncollectedCompletions sweep and every operator 'Retry charge' hits the guard at line 67-81, sees the failed row's PI, and returns ok:true 'Already charging' -- no new debit is ever created, the sweep records drift_repaired, and $300 is never collected. Identical guard in chargeSelfPayAchAppointment.ts:69-83 blocks self-pay ACH retries.
    evidence: chargeAchAppointment.ts:67-81: `.eq('payment_type','revenue').not('stripe_payment_intent_id','is',null).limit(1); if (piRows && piRows.length > 0) return { ok: true, code: 'processing', message: 'Already charging', ... }` -- no status filter, so a status='failed' row matches. Same block in chargeSelfPayAchAppointment.ts:69-83. Sweep loop that keeps

--- [CRITICAL] Failed SELF-PAY payouts are retried through the tenant-split settlement, paying the org a phantom 'tenant remainder' from the platform balance  => UNVERIFIED (verifier never ran)
    file: src/lib/payments/reconcile.ts:463  category: money-correctness
    summary: retryFailedPayouts and the manual /api/payouts/[payoutId]/retry route both call settleCleanerPayout for every failed/pending payout row with no is_self_pay filter. A failed self-pay payout row (written by settleSelfPay with payout_percent_snapshot set, so it passes the sweep's filter) then runs the WRONG settlement: settleCleanerPayout computes a tenant remainder from the self-pay payment row (splitBase = grossed-up charge minus processing_fee_cents = cleanerCut + platformFee) and executes a real platform->tenant transfer under the never-before-used key tenant-payout-{id}, then pays the cleaner the carved cut. For a self-pay charge there IS no tenant remainder -- the platform only ever netted cut + fee -- so the org receives free money and the platform balance goes negative by roughly (cut+fee)*(1 - payout%).
    scenario: Self-pay $200 job, cleaner 50%, fee 1%: org charged ~$104.93 grossed-up (cut $100 + fee $2 + Stripe fee). settleSelfPay's cleaner transfer fails transiently -> payouts row {status:'failed', amount:100, payout_percent_snapshot:50, is_self_pay:true}. Admin clicks Retry in the triage band (or the sweep runs): settleCleanerPayout computes splitBase = $102, cleanerCents=$51... but pays the carved $100 to the cleaner AND FIRST transfers tenantRemainder = 10200 - 102 - 5100 = $49.98 to the org's connected account (key tenant-payout-{id}, fresh, so Stripe executes it). Platform netted $102 from the charge but pays out $149.98: ~$48 lost per occurrence, and the org is silently gifted money it already spent.
    evidence: reconcile.ts:460-466: `.from('payouts').select(...).in('status', ['failed','pending']).not('payout_percent_snapshot','is',null)` -- no is_self_pay exclusion; :473 `settleCleanerPayout(supabase, row.appointment_id, null)`. Manual path: src/app/api/payouts/[payoutId]/retry/route.ts:98 `settleCleanerPayout(supabaseAdmin, payout.appointment_id, null)` 

--- [HIGH] Homeowner ACH failure is invisible: no triage entry, no notification, and the booking sheet claims it will be charged automatically  => UNVERIFIED (verifier never ran)
    file: src/lib/payments/dispatchStripeEvent.ts:392  category: visibility
    summary: A pre-settlement ACH failure (payment_intent.payment_failed days after completion) only sets payments.status='failed'. It never stamps appointments.authorization_status and only emits an admin notification when metadata.self_pay==='true', so a homeowner-billed ACH bounce produces zero notifications. The admin triage band selects failed charges by appointments.authorization_status IN ('failed','requires_action') -- misses it. derivePaymentSectionState has no branch for paymentStatus==='failed' (only authorizationStatus), so the booking sheet renders 'before_charge', whose completed-job hint reads 'This cleaning is complete. It will be charged on the next billing run' -- which is false, because finding 1 means it never will be.
    scenario: Homeowner pays a completed $250 job by bank; the debit bounces on day 4. payments.status='failed', appointments.authorization_status stays NULL. Admin's Payments 'Needs you now' band: empty. Bell/email: nothing (the notify block at dispatchStripeEvent.ts:413 requires self_pay==='true'). Booking sheet payment section: neutral 'Card on file' state with 'will be charged on the next billing run'. Homeowner recovery banner (keys on authorization_status='failed'): absent. Nobody ever learns the $250 was not collected.
    evidence: dispatchStripeEvent.ts:392-395 (`.update({ status: 'failed' }).eq('stripe_payment_intent_id', ...)` -- payments only, no appointments update); :413 `if (paymentIntent.metadata?.self_pay === 'true')` gates the only notification. usePaymentsTriage.ts:94 `.in('authorization_status', ['failed','requires_action'])`. paymentSectionState.ts:24-31 maps pay

--- [HIGH] settleSelfPay recomputes the cleaner cut from live payout_percent at settlement instead of a charge-time snapshot  => UNVERIFIED (verifier never ran)
    file: src/lib/payments/settleSelfPay.ts:150  category: money-correctness
    summary: The self-pay charge amount is fixed at charge time (grossUp(cut@T0 + fee@T0)), but settlement re-derives the cut from the CURRENT cleaner_profiles.payout_percent and appointments.total_price when payment_intent.succeeded arrives. For self-pay ACH that is ~4 business days later, so an edit to the cleaner's percent (or the job price) in that window makes the transfer diverge from what the org funded: a raise overdraws the platform (transfer > what was netted, silently consuming/negating the platform fee), a cut means the org overpaid and the platform silently keeps the difference. settleCleanerPayout explicitly snapshots to prevent exactly this class (its comment: 'recomputing would over/underpay the cleaner and strand funds'); settleSelfPay has no equivalent.
    scenario: Self-pay ACH job, $200, cleaner at 50% -> org debited grossUp($100 cut + $2 fee) = $102.83. On day 2 the admin raises the cleaner to 80%. Day 4 the debit settles; settleSelfPay computes cleanerCutCents = floor(20000*0.80) = $160 (the Math.min cap at line 155 binds only against the $102.83 captured amount minus refunds, so $102.83 is transferred, not $100). Platform pays out more than the cut it collected, the $2 platform fee inverts into a loss, and no ledger flags the mismatch (checkMoneyMathInvariants validates against the payout's own snapshot).
    evidence: settleSelfPay.ts:150-155: `const payoutPercent = Number(cleaner!.payout_percent); const jobGrossCents = Math.round(Number(appt.total_price) * 100); const { cleanerCutCents: fullCutCents } = computeSelfPayAmounts({ jobGrossCents, payoutPercent }); const cleanerCutCents = Math.min(fullCutCents, Math.max(0, capturedCents - refundedCents));` vs charge-

--- [HIGH] Self-pay settlement with an unpayable cleaner strands the funds with no payout row, no retry, and no UI surface  => UNVERIFIED (verifier never ran)
    file: src/lib/payments/settleSelfPay.ts:137  category: money-correctness
    summary: If the cleaner became unpayable between the self-pay charge and settlement (Connect account restricted via account.updated, payout_percent zeroed, switched to hourly_external -- all possible in the multi-day ACH window), settleSelfPay records a cleaner_transfer_failed payment_event and returns WITHOUT creating a payouts row. Every automated backstop needs one: retryFailedPayouts selects from payouts, the triage band's failed/held sections query payouts, and settleUnsettledCaptures explicitly excludes self-pay rows. The webhook event is marked processed, so nothing ever re-invokes settleSelfPay. The org's money sits on the platform balance indefinitely; the cleaner is never paid; no screen shows it.
    scenario: Org self-pay $150 ACH job charged at completion. During the 4-day clearing window Stripe restricts the cleaner's Connect account; account.updated flips stripe_connect_onboarding_complete=false. payment_intent.succeeded -> settleSelfPay -> cleanerPayable=false -> ledger event only, return {settled:false}. Payment row reads 'paid'. No payout row exists, so retryFailedPayouts (reconcile.ts:460-466) and usePaymentsTriage's failed/held queries find nothing; settleUnsettledCaptures skips self-pay (reconcile.ts:388). When the cleaner re-onboards, nothing re-triggers settlement. $150 minus fees is stranded on the platform forever unless someone replays the webhook by hand.
    evidence: settleSelfPay.ts:130-148: `if (!cleanerPayable) { await recordPaymentEvent(... 'cleaner_transfer_failed' ...); return { settled: false, reason: 'cleaner_not_payable' }; }` -- no upsertPayout call (contrast settleCleanerPayout.ts:301-312 which HOLDS a 'pending' payout row for this case). reconcile.ts:388 `.or('is_self_pay.is.null,is_self_pay.eq.fals

--- [MEDIUM] Self-pay OFF-flag gating is data-dependent, not code-enforced: charge orchestration and the redesign booking form ignore STRIPE_SELF_PAY_ENABLED  => UNVERIFIED (verifier never ran)
    file: src/lib/payments/chargeCompletedAppointment.ts:171  category: flag-gating
    summary: The org payment-method routes 404 correctly when the flag is off, but the server-side self-pay CHARGE path does not check stripeSelfPayEnabled() anywhere: chargeCompletedAppointmentAuto routes on appointments.is_self_pay alone (gated only by STRIPE_NEW_CHARGE_FLOW_ENABLED, which is ON in prod), and chargeUncollectedCompletions explicitly includes is_self_pay rows under the same single flag. Meanwhile the redesign OperatorBookingForm renders the 'Company pays' toggle unconditionally (the legacy AddAppointmentModal gates it behind stripeSelfPayUiEnabled(); the redesign form -- the live prod UI post-cutover -- does not), and the appointment insert is a client-side Supabase write with no server flag check. Today the money movement is blocked only because prod orgs have stripe_self_pay_customer_id NULL (the charge bails with no_org_card), i.e. the gate is data state, not the flag.
    scenario: Prod today (self-pay OFF, new-charge-flow ON): an operator opens the redesign New Booking sheet and sees 'Company pays'. Selecting it dead-ends (the org methods route 404s so selfPayHasMethod stays false) -- a confusing dead toggle. If an is_self_pay=true appointment ever exists (client-side insert isn't flag-checked, or a row created while the flag was on in an environment), the completion charge and the reconcile sweep will run the full self-pay company-card path with the flag off; the only thing preventing a charge is the org lacking a self-pay Customer id. Flipping the flag off after any org has saved a company card does NOT stop self-pay charging.
    evidence: chargeCompletedAppointment.ts:171-173 `appt.is_self_pay ? await chargeSelfPayNow(...)` with no stripeSelfPayEnabled() check anywhere in chargeSelfPayNow (:402-519; the only flag check is the ACH sub-branch at :466). Charge route gate is only `stripeEnabled() && stripeNewChargeFlowEnabled()` (src/app/api/appointments/[appointmentId]/charge/route.ts:

--- [MEDIUM] org/confirm-setup-intent first-save adopts whatever Customer the caller-named SetupIntent carries, allowing a homeowner's card to become the org's self-pay funding source  => UNVERIFIED (verifier never ran)
    file: src/app/api/stripe/org/confirm-setup-intent/route.ts:59  category: authz
    summary: The cross-org ownership check only runs when the org already has a stored stripe_self_pay_customer_id: `if (org.stripe_self_pay_customer_id && ... !== customerId) 403`. When it is NULL (org never called create-setup-intent, or that write failed), the route attaches the PM to and PERSISTS whatever Customer is on the supplied SetupIntent. Org staff can obtain succeeded homeowner SetupIntent ids from their own admin card-collection flows (the homeowner create-setup-intent response returns setup_intent_id), so a malicious/buggy client can wire a HOMEOWNER's Stripe Customer in as the org's self-pay Customer; thereafter every 'company pays' job charges that homeowner's saved card. The SetupIntent carries no org metadata (createSetupIntent sets only source), so provenance cannot be verified even in principle without adding it.
    scenario: Self-pay enabled. Org admin (who has never set up a company card, so stripe_self_pay_customer_id is NULL) collects a homeowner's card via the normal admin flow and notes the returned setup_intent_id. They POST it to /api/stripe/org/confirm-setup-intent: setupIntent.customer is the homeowner's cus_..., the guard at :59 is skipped (stored id null), the PM attaches, and :65-69 persists the homeowner's Customer id as the org's self-pay Customer. Every subsequent self-pay completion (chargeSelfPayNow reads organizations.stripe_self_pay_customer_id) bills the homeowner's card for the company's own cleanings.
    evidence: route.ts:59-61: `if (org.stripe_self_pay_customer_id && org.stripe_self_pay_customer_id !== customerId) { return ... 403 }` -- null stored id bypasses; :63-69 attaches and persists `customerId` from the client-supplied SetupIntent. src/lib/stripe.ts:106-121 createSetupIntent sets metadata `{ source }` only (no organization_id to verify against). Le

--- [MEDIUM] Booking sheet hides all self-pay charge states: failed/processing/paid all render as a bare 'Self-pay' badge with no recovery actions  => UNVERIFIED (verifier never ran)
    file: src/lib/payments/paymentSectionState.ts:24  category: visibility
    summary: derivePaymentSectionState short-circuits `if (isSelfPay) return 'self_pay'` BEFORE the failed/requires_action/paid/processing branches, so the operator booking sheet shows 'Self-pay -- Managed in Settings, Payments' regardless of whether the company-card charge declined, an ACH debit is mid-clearing, or the job is fully paid. The per-state recovery actions the section offers for homeowner rows (Retry charge, etc.) are unreachable for self-pay from the booking detail. A failed self-pay CARD charge is still findable in the triage band (recordChargeDecline stamps authorization_status), but a failed self-pay ACH debit appears nowhere (finding 3's mechanics), leaving the admin notification as the only breadcrumb with no working follow-up action.
    scenario: Org self-pay job's company card declines at completion: appointments.authorization_status='failed', payments.status='failed'. Admin opens the booking to investigate: the Payment section shows the neutral 'Self-pay' badge and 'Managed in Settings, Payments' -- no Failed state, no Retry button. For the ACH variant, even the triage band is empty, so after the one-time bell notification is dismissed there is no screen anywhere showing this job's money failed.
    evidence: paymentSectionState.ts:23-26: `if (isSelfPay) return 'self_pay'; if (authorizationStatus === 'failed') return 'failed';` -- order guarantees self-pay masks failure. OperatorPaymentSection.tsx:38,48: self_pay renders `{ tone: 'selfpay', label: 'Self-pay' }` / 'Managed in Settings, Payments.' Failed self-pay card IS in triage via usePaymentsTriage.ts

--- [MEDIUM] STRIPE_ACH_ENABLED without STRIPE_FEE_PASSTHROUGH_ENABLED makes the platform silently eat the ACH fee on every debit (documented dependency, unenforced)  => UNVERIFIED (verifier never ran)
    file: src/lib/payments/chargeAchAppointment.ts:104  category: money-correctness
    summary: flags.ts documents that ACH 'depends on the new charge flow and fee passthrough', but no code enforces it: chargeHomeownerNow routes to ACH on stripeAchEnabled() alone, and chargeAchAppointment quietly degrades to chargeCents=baseCents, feeCents=0, processing_fee_cents=null when passthrough is off. Settlement then splits the FULL captured amount to tenant+cleaner (splitBase gets no fee deduction), so Stripe's 0.8%/$5-cap ACH fee comes out of the platform balance on every ACH job -- recreating the exact negative-balance drift the platform-fee work just fixed, offset only partially by the 1% fee.
    scenario: Ops enables STRIPE_ACH_ENABLED in prod but forgets STRIPE_FEE_PASSTHROUGH_ENABLED (nothing warns; both booking and charging work normally). Every $500 ACH job: platform nets $496 after Stripe's $4 fee but transfers out the full $500 split minus the $5 platform fee -> net +$1 instead of +$5; larger jobs cap at -$5+fee. Across a fleet of ACH jobs the platform balance bleeds exactly as it did pre-migration-111, with no alert (the balance-floor monitor is still an open follow-up).
    evidence: chargeCompletedAppointment.ts:332: `if (stripeAchEnabled() && (await getPaymentMethodType(paymentMethodId)) === 'us_bank_account')` -- no passthrough check. chargeAchAppointment.ts:104-107: `const passthrough = stripeFeePassthroughEnabled(); const { chargeCents, feeCents } = passthrough ? computeChargeBreakdown('us_bank_account', baseCents) : { cha

--- [MEDIUM] Synchronous ACH charge-create failure leaves no payment row, no ledger event, and no notification; the sweep retries the same spent idempotency key silently forever  => UNVERIFIED (verifier never ran)
    file: src/lib/payments/chargeSelfPayAchAppointment.ts:163  category: visibility
    summary: When createAchCharge/createSelfPayAchCharge throws at create+confirm time (dead bank PM, revoked mandate, Stripe 4xx), both ACH orchestrations return {ok:false, code:'failed'} with no DB write of any kind -- unlike the card path's recordChargeDecline, which stamps authorization_status, writes a failed payment row, ledgers, and notifies. 'failed' is not in PRECONDITION_CODES, so the charge claim releases authorization_status to NULL, putting the appointment straight back into chargeUncollectedCompletions. The ACH idempotency keys have no reauth suffix (`ach-charge-{id}` / `selfpay-ach-{id}`), so every sweep retry replays the same failed request. Result: an endless, invisible retry loop with zero operator surface (the one-time HTTP error response is the only signal, and the sweep's failures go to console.error only).
    scenario: Homeowner's saved bank PM is detached at the bank (Financial Connections revoked). Job completes; createAchCharge throws. Route returns 502 once. No payment row, no payment_event, no notification, authorization_status back to NULL. Every 30-minute sweep re-attempts, replays the same idempotent failure, logs to console, and moves on. The job never surfaces in triage (no authorization_status, no failed payment row) and the money is never collected until someone stumbles on it.
    evidence: chargeAchAppointment.ts:119-121 and chargeSelfPayAchAppointment.ts:163-165: `catch (err) { return { ok: false, code: 'failed', message: ... }; }` -- no recordPaymentEvent/notify/row write (contrast recordChargeDecline, chargeCompletedAppointment.ts:597-644). chargeCompletedAppointment.ts:72-79 PRECONDITION_CODES omits 'failed', so the finally at :1


================================================================================
## DOMAIN: webhooks-reconcile

COVERAGE: Read in full: src/app/api/stripe/webhook/route.ts (119 lines), src/lib/payments/webhookIdempotency.ts, dispatchStripeEvent.ts (1354 lines, all 25 handled event types), events.ts, reconcile.ts (all 8 sweep jobs), settleCleanerPayout.ts, settleSelfPay.ts, clawback.ts, refundCancelledCharge.ts, src/lib/stripe/reconcile.ts, src/lib/stripe/charges/splits.ts (partial), src/lib/monitoring/platformAlert.ts, src/app/api/cron/reconcile-payments/route.ts, src/app/api/stripe/connect/reconcile-payouts/route.ts, migrations 065/067/085/111 (relevant sections). Grepped all UI/hook consumers of payment_events, platform_alerts, disputes, and reconcile-payouts.

SOLID: Claim semantics are correct at the core - insert-first on webhook_events PK, only 23505 triggers the duplicate check, non-23505 errors throw so the route 500s and Stripe retries (webhookIdempotency.ts:45-47); markWebhookProcessed throws on failure so a silently-dropped mark can't cause 200+reprocess (69-80). Livemode-vs-key-mode check (route.ts:74-80) and dual-secret verification are good. Out-of-order hardening is genuinely strong: refund-before-succeeded guard (dispatchStripeEvent.ts:147-165), late ACH failure after paid/refunded (354-388), refund.failed reverting 'refunded' to 'paid' with coverage recompute (505-538), settlement shrinking by Stripe amount_refunded (settleCleanerPayout.ts:92-125). Transfer idempotency keys (tenant-payout-/cleaner-payout-/selfpay-cleaner-), the payout terminal-status guards, the carved-slice snapshot on retry, and the transfer-id repair path (settleCleanerPayout.ts:229-257) make the homeowner-card settle path safely replayable. CRON auth fails closed (route.ts:44-50). Dispatch table handles the right money events; unhandled types are only logged (charge.dispute.funds_withdrawn/reinstated are the notable absentees). Tests exist: 9 unit (claim semantics), 27 webhook integration, 8 cron integration.

VERDICT on "DB state never depends on a single webhook delivery": TRUE for the homeowner card flow (charge -> settle -> payout -> clawback all have idempotent sweeps: reconcileStuckPayments, settleUnsettledCaptures, retryFailedPayouts, retryStrandedClawbacks). FALSE for four money states: (1) connected-account events (payout.paid/failed, account.updated) - the dead-letter retry re-fetches events without the Stripe-Account header so it can NEVER recover them (finding 3); (2) the payouts bank-settlement lifecycle - no sweep covers paid->bank_paid at all, prod doesn't even subscribe the events, and the manual reconcile route has zero callers (finding 6); (3) self-pay settlement when the cleaner isn't payable - no payout row is written so no sweep ever retries it (finding 5); (4) refunds-row terminal-status sync depends solely on refund.updated/charge.refunded deliveries (mitigated: the pending-counts-toward-cap direction is safe, and payment-level status has amount_refunded backstops).

VISIBILITY: Split verdict. Day-to-day chargeable failures ARE visible and actionable: PaymentsNeedingAttentionSection surfaces authorization_status failed/requires_action appointments, failed payments, and failed payouts; bell notifications exist for authorization_failed, dispute_opened, refund_failed, clawback_blocked, cancelled_job_refunded, and cleaner_paid. But everything this domain's backstop DISCOVERS is invisible: payment_events (money_math_violation, tenant_transfer_failed, cleaner_clawback_failed, unmatched_dispute, early_fraud_warning, radar_review_opened, setup_intent_failed) is written by 3 lib files and read by ZERO UI components, hooks, or API routes - it is a write-only table. recordPlatformAlert is called from exactly one place in the codebase (forgot-password email health) and never from any money code; platform_alerts likewise has no UI reader. The cron sweep's JSON result (violation counts, stillFailed dead letters) is discarded by pg_net (migration 067 fires and forgets), so a permanently broken sweep, a growing dead-letter queue, or a money-math violation pages no one. The disputes table is written by the webhook but read by nothing - the admin gets one bell ping with a due date and then has no screen showing dispute status, amount, or evidence deadline, and as a Connect Express platform the tenant cannot act in any Stripe dashboard either. Homeowner visibility in this domain is essentially nil (all audiences are 'admin' or 'cleaner'), which is acceptable for webhook/reconcile plumbing except disputes/refund confirmations. Bottom line: the operator can see what the happy-path UI chooses to show, but is blind to exactly the class of failures the reconciler exists to catch.

--- [HIGH] retryFailedPayouts routes self-pay payouts through the tenant-split path, wiring a tenant transfer that must not exist  => CONFIRMED (high, severity=high)
    file: src/lib/payments/reconcile.ts:460  category: money-correctness
    summary: The failed-payout sweep selects payouts in ('failed','pending') with no is_self_pay filter and replays them through settleCleanerPayout, which contains zero self-pay guards (grep confirms no is_self_pay reference in the file). For a self-pay job, settleCleanerPayout's tenant leg runs BEFORE any prior-payout terminal check (settleCleanerPayout.ts:170-200) and transfers the 'tenant remainder' of the grossed-up captured amount back to the org under a fresh tenant-payout-{id} idempotency key - money the platform captured precisely to keep (self-pay: only the cleaner cut should ever leave the platform, per settleSelfPay's own doc). settleUnsettledCaptures explicitly excludes self-pay for this exact reason (reconcile.ts:368-372) but retryFailedPayouts does not. Also, the cleaner leg then pays under cleaner-payout-{id}, a DIFFERENT key from settleSelfPay's selfpay-cleaner-{id}, so a crash-after-transfer scenario double-pays the cleaner. Self-pay is flag-OFF in prod today, which is the only thing keeping this from critical.
    scenario: Self-pay enabled; org completes a $120 self-pay job; cleaner's connected account is briefly restricted so settleSelfPay's transfer throws and writes a payouts row status='failed' with is_self_pay=true (settleSelfPay.ts:193). 15 minutes later the cron sweep runs retryFailedPayouts -> settleCleanerPayout -> tenant transfer of roughly gross+fee-cleaner%-fee (~$85) to the org's connected account, which the org was never owed. Platform balance drained ~$85 per incident, recorded nowhere visible.
    evidence: reconcile.ts:460-475: `.from('payouts').select('id, appointment_id').in('status', ['failed', 'pending'])... const result = await settleCleanerPayout(supabase, row.appointment_id, null);` - no is_self_pay filter. settleCleanerPayout.ts:39-43 selects `cleaner_id, organization_id, total_price, status` (no is_self_pay), and lines 170-179 run `createPla
    verdict: Verified end-to-end. retryFailedPayouts (src/lib/payments/reconcile.ts:460-475) selects payouts in ('failed','pending') with only payout_percent_snapshot/appointment_id null-filters; a failed self-pay payout row written by settleSelfPay (settleSelfPay.ts:164-165, 193) carries a non-null snapshot and is therefore selected — no is_self_pay filter exists. It replays through settleCleanerPayout, which contains zero self-pay awareness (appointments se
    correction: The mechanism and severity are correct, but the failure scenario's dollar magnitude is overstated. A self-pay charge captures only grossUp(cleanerCut + platformFee) (selfPayMath.ts:86-91), not the grossed-up full job price, and processing_fee_cents is recorded on the payment row (chargeCompletedAppo

--- [HIGH] handleTransferReversed marks a payout terminally 'reversed' on ANY transfer.reversed, including partial reversals, then silently blocks future full clawbacks  => CONFIRMED (high, severity=high)
    file: src/lib/payments/dispatchStripeEvent.ts:960  category: money-correctness
    summary: Stripe fires transfer.reversed for partial reversals too. The in-app partial-refund path (reverseJobTransfersForRefund) correctly flips the payout to 'reversed' only when fully reversed (clawback.ts:313-318), but the webhook echo of that same partial reversal then unconditionally sets status='reversed' (dispatchStripeEvent.ts:960-963). 'reversed' is a hard terminal state: clawbackCleanerPayout returns alreadyReversed with no ledger event (clawback.ts:112) and settleCleanerPayout treats it as never-re-pay (settleCleanerPayout.ts:229). So after any partial refund, a later dispute-lost or ACH-return clawback of the cleaner's remaining cut is silently skipped and the platform eats it. Prod's live webhook does not subscribe transfer.reversed today, but the cutover runbook adds it - this arms the bug at cutover.
    scenario: $100 job, cleaner paid $30. Admin issues a $20 partial refund -> reverseJobTransfersForRefund reverses $6 of the cleaner transfer (correctly leaves status='paid') -> Stripe emits transfer.reversed -> webhook marks the payout 'reversed'. Weeks later the ACH debit returns (payment_intent.payment_failed) -> clawbackCleanerPayout hits `if (payout.status === 'reversed') return { alreadyReversed: true }` -> the remaining $24 stays with the cleaner, no ledger event, no alert, platform absorbs the loss.
    evidence: dispatchStripeEvent.ts:960-963: `await supabase.from('payouts').update({ status: 'reversed', reversed_at: ... }).eq('id', existingPayout.id);` with no check of transfer.amount_reversed vs transfer.amount. clawback.ts:112: `if (payout.status === 'reversed') return { ...base, alreadyReversed: true };` (returns before the Stripe-side remaining-amount 
    verdict: Verified line-by-line. handleTransferReversed (src/lib/payments/dispatchStripeEvent.ts:946-967) unconditionally sets payouts.status='reversed' on any transfer.reversed event without comparing transfer.amount_reversed to transfer.amount, and Stripe fires that event for partial reversals. The in-app partial-refund unwind (src/lib/payments/clawback.ts:296-319) deliberately reverses only a proportional slice of the cleaner transfer and only flips sta
    correction: Minor tempering of the auditor's 'silently blocks future full clawbacks' framing: only the clawbackCleanerPayout family (dispute_lost, ach_return, reconcile_retry) is blocked. The refund-unwind path (reverseJobTransfersForRefund) does not read payout.status, so subsequent partial/full homeowner refu

--- [HIGH] Dead-letter retry can never recover connected-account events (no Stripe-Account header), and unrecoverable rows starve the FIFO batch forever  => CONFIRMED (high, severity=high)
    file: src/lib/payments/reconcile.ts:72  category: idempotency
    summary: retryDeadLetterWebhooks selects only `id` from webhook_events and calls retrieveStripeEvent(row.id), which is `getStripe().events.retrieve(eventId)` with no stripeAccount option (src/lib/stripe/reconcile.ts:12-14). Connect-delivered events (payout.paid/failed, payout.canceled, account.updated - anything with event.account) live on the connected account and 404 when retrieved from the platform, so any such row stuck in received/failed is permanently unrecoverable - even though claimWebhookEvent already persists account_id (webhookIdempotency.ts:35) for exactly this purpose. Compounding: events older than Stripe's 30-day retention also 404 forever, webhook_events has no terminal 'dead' status (migration 065:344 allows only received/processed/failed), and the sweep orders by received_at ASCENDING with limit 100 - so once 100 permanently-dead rows accumulate, every 15-minute sweep burns its whole batch on them and NEWER recoverable events are never retried. Note the route also tells Stripe 'duplicate' (200) for a crashed-but-recent 'received' row (route.ts:96-99), making the sweep the ONLY recovery path for that event - a path that is broken for Connect events.
    scenario: A worker crashes 5s into processing payout.failed for a cleaner (row stays 'received'); Stripe's retry 30s later gets duplicate:true/200 and stops redelivering. The sweep picks the row up, events.retrieve 404s on the platform account, markWebhookFailed - forever, every 15 minutes. The cleaner's payout row stays falsely 'bank_paid' with no recovery path. Meanwhile 100+ such rows accumulate over months and the ascending-ordered batch never reaches a newer, recoverable payment_intent.succeeded dead letter.
    evidence: reconcile.ts:58-64: `.select('id').neq('status', 'processed').lte('received_at', cutoff).order('received_at', { ascending: true }).limit(batch)`; reconcile.ts:72: `const event = await retrieveStripeEvent(row.id);` vs src/lib/stripe/reconcile.ts:13: `return getStripe().events.retrieve(eventId);` (single arg). webhookIdempotency.ts:35 stores `account
    verdict: Every load-bearing claim checks out in code. (1) src/lib/payments/reconcile.ts:58-72 selects only `id` from webhook_events and calls retrieveStripeEvent(row.id); src/lib/stripe/reconcile.ts:12-14 is `getStripe().events.retrieve(eventId)` with no stripeAccount option — grep confirms neither `account_id` nor `stripeAccount` appears anywhere in the sweep or the wrapper, even though claimWebhookEvent persists account_id (webhookIdempotency.ts:35, fro
    correction: Two sub-claims need tightening. (a) The "Stripe retry 30s later gets duplicate/200 and stops redelivering" leg only holds when the retry lands inside the 60s IN_FLIGHT_WINDOW_MS (webhookIdempotency.ts:20,66); a retry after 60s reclaims and reprocesses ('received'+stale → claimed), and Stripe's live-

--- [HIGH] The reconciler's findings alert no human: payment_events is a write-only table, recordPlatformAlert is never called from money code, and the cron result is discarded  => CONFIRMED (high, severity=high)
    file: src/lib/payments/reconcile.ts:546  category: visibility
    summary: checkMoneyMathInvariants 'flags' violations by writing money_math_violation rows to payment_events - but grep across src/app, src/components, src/hooks shows ZERO readers of payment_events (only writers in lib/payments and the org-wipe route). The same void swallows tenant_transfer_failed, cleaner_clawback_failed, unmatched_dispute, early_fraud_warning, radar_review_opened, and setup_intent_failed. recordPlatformAlert (which has a working ALERT_WEBHOOK_URL Slack sink and platform_alerts persistence) is imported by exactly one file in the codebase: api/auth/forgot-password. The cron route returns violation/stillFailed counts in its JSON, but pg_cron calls it via net.http_post which ignores the response (migration 067:27-34), so a sweep that 500s every cycle, a dead-letter queue growing unboundedly, or a wrong cleaner payout is detected and then told to nobody. This directly breaks the 'high = invisible failed money states' bar: the states are detected, recorded, and invisible.
    scenario: A paid cleaner payout drifts from the locked split (e.g. the finding-1 self-pay wrong-path fires, or a legacy transfer overpaid). checkMoneyMathInvariants catches it on the next sweep and writes money_math_violation. Nothing renders that table, no webhook/SMS/email fires, the cron caller discards the count. The platform owner learns about the drift from the Stripe balance page weeks later - exactly the prod -$4.48 negative-balance class of incident this system was built to prevent.
    evidence: reconcile.ts:546-559: violations only `await recordPaymentEvent(... eventType: 'money_math_violation' ...)`. Grep: `from('payment_events')` appears only in lib/payments/{reconcile,events,clawback}.ts; `recordPlatformAlert` call sites: only src/app/api/auth/forgot-password/route.ts:50. Migration 067_reconcile_payments_cron.sql:27: `SELECT net.http_p
    verdict: Core claims all verified: reconcile.ts:546-559 records money_math_violation only to payment_events; no UI/hook/component anywhere reads payment_events (readers are only the sweep's own retry selectors in lib/payments and the org-wipe delete at api/platform/organizations/[id]/route.ts:273); recordPlatformAlert (with its working ALERT_WEBHOOK_URL sink) is called solely from api/auth/forgot-password/route.ts:50 and nothing reads platform_alerts; mig
    correction: The "same void swallows" list is overstated for three of the six named event classes. Cleaner-side failures are visible and actionable: settleCleanerPayout.ts:302/326 writes payouts.status 'pending'/'failed' rows that the redesign admin triage UI reads directly (src/components/redesign/payments/useP

--- [HIGH] Self-pay charge captured while cleaner is unpayable strands the funds with no automated retry  => CONFIRMED (high, severity=high)
    file: src/lib/payments/settleSelfPay.ts:137  category: money-correctness
    summary: When settleSelfPay finds the cleaner not payout-capable (disconnected, onboarding incomplete, percent 0), it records a cleaner_transfer_failed ledger event and returns - WITHOUT writing a payouts row (contrast settleCleanerPayout, which holds a 'pending' payout row precisely so the sweep can retry). Every backstop then misses it: retryFailedPayouts needs a payouts row (reconcile.ts:460-466); settleUnsettledCaptures explicitly excludes self-pay (reconcile.ts:388); chargeUncollectedCompletions skips it because the revenue row is 'paid'; and the only trigger for settleSelfPay is the single payment_intent.succeeded delivery. The org has been charged, the cleaner is owed money, and the funds sit on the platform balance until someone manually intervenes - guided only by a payment_events row nothing displays (finding 4). Directly falsifies the 'never depends on a single webhook delivery' claim for this state. Self-pay is flag-OFF in prod, so this must be fixed before re-enable.
    scenario: Org books a self-pay clean; cleaner's Connect account gets restricted between booking and completion. Completion charge succeeds ($126 grossed-up captured). payment_intent.succeeded -> settleSelfPay -> cleanerPayable=false -> ledger event only, return. Cleaner re-onboards the next day; nothing re-fires settleSelfPay (account.updated doesn't, no payout row exists for the sweep). Cleaner never paid; $126 stranded on platform; org believes the job is fully paid.
    evidence: settleSelfPay.ts:137-148: `if (!cleanerPayable) { await recordPaymentEvent(... eventType: 'cleaner_transfer_failed' ...); return { settled: false, reason: 'cleaner_not_payable' }; }` - no payouts upsert (the failed-transfer branch at 193 DOES write one). reconcile.ts:460-466 sweep source is `.from('payouts')`; reconcile.ts:388 excludes self-pay fro
    verdict: Verified line-by-line and could not refute. settleSelfPay.ts:137-148 handles cleaner-not-payable with only a payment_events ledger entry and no payouts row, unlike (a) its own failed-transfer branch at 192-204 (writes a 'failed' payout row) and (b) settleCleanerPayout.ts:301-313 (holds a 'pending' row precisely so the sweep retries). Every backstop then provably misses the state: retryFailedPayouts sources from payouts (reconcile.ts:460-466, noth

--- [MEDIUM] The paid->bank_paid payout lifecycle has no backstop: events unsubscribed in prod, reconcile route has zero callers, cron sweep has no bank-paid job  => CONFIRMED (high, severity=medium)
    file: src/app/api/stripe/connect/reconcile-payouts/route.ts:7  category: visibility
    summary: Three facts compound: (1) CLAUDE.md documents the prod live webhook is missing transfer.reversed, payout.paid, and payout.failed, so handlePayoutPaid/handlePayoutFailed never run in prod; (2) the manual fallback /api/stripe/connect/reconcile-payouts is called by NOTHING - grep finds zero references outside the route file itself (dead code, and it is cleaner-self-auth'd anyway, so admins couldn't trigger it); (3) the cron sweep's job list has no paid->bank_paid reconciliation. Consequence A (visibility): cleaner payouts in prod can never reach bank_paid, so CleanerPayoutsHistory/derivePayments show transfers as perpetually 'paid' with no deposited-to-bank truth, and a bank payout FAILURE never reverts anything. Consequence B (money): clawbackCleanerPayout's bank_paid 'blocked' guard (clawback.ts:118-150) - which exists to stop reversals after funds left Stripe from driving connected balances negative - can never engage, because no row is ever bank_paid.
    scenario: Prod today: a cleaner's bank account is closed; Stripe's payout to them fails. No payout.failed subscription -> DB keeps status='paid' (and would keep bank_paid if it ever got set). Admin and cleaner both see a healthy paid payout; the cleaner asks where their money is; support has nothing in-app to look at. Separately, an ACH return triggers a full clawback of a payout whose funds already reached the bank - the blocked-guard never fires and the cleaner's connected balance goes negative with no in-app trace.
    evidence: Grep `reconcile-payouts` across src/ and tests/: only the route file matches - no fetch caller in any hook/component. cron route.ts:53-62 job list: deadLetter, stuckPayments, stuckCharging, uncollectedCompletions, unsettledCaptures, failedPayouts, strandedClawbacks, moneyMath - none touch bank_paid. CLAUDE.md 'Stripe webhook coverage by environment
    verdict: All three core facts verified: (1) prod live webhook lacks payout.paid/payout.failed/transfer.reversed (CLAUDE.md, corroborated by docs/payments-sweep-test-plan.md G14 and by dispatchStripeEvent.ts:1017's own warning); (2) /api/stripe/connect/reconcile-payouts has zero callers anywhere in the repo — its former Earnings-page caller was removed in PR #46 ("standardize balance/payout UI on Stripe embed"), and it is cleaner-self-auth'd (route.ts:28-3
    correction: Core defect is accurately stated: prod has no live path to bank_paid (webhook events unsubscribed, fallback route dead since PR #46 removed its only caller, no cron job), so the per-job payout lifecycle silently terminates at 'paid', payout.failed can never revert anything in the DB, and the clawbac

--- [MEDIUM] payment_intent.canceled never terminalizes the payments row, so reconcileStuckPayments re-selects it forever and logs false drift_repaired rows every 15 minutes  => CONFIRMED (high, severity=medium)
    file: src/lib/payments/dispatchStripeEvent.ts:455  category: money-correctness
    summary: handlePaymentIntentCanceled updates only payment_intent_status, leaving payments.status='pending'. reconcileStuckPayments selects status='pending' rows past SLA, maps pi.status 'canceled' to a replay of payment_intent.canceled, records a drift_repaired ledger event, dispatches the synthetic event - which again changes nothing - and counts it repaired. The row is re-selected on every subsequent sweep: one Stripe API read plus one false 'drift_repaired' payment_events row per 15 minutes, forever, per canceled PI. The admin payments UI shows the row as pending indefinitely, and the ever-growing repaired count masks real drift in the sweep's (already unread) output.
    scenario: An operator cancels a stuck PaymentIntent in the Stripe Dashboard. The payments row stays 'pending'. Every 15 minutes for the life of the system: retrievePaymentIntent + recordPaymentEvent('drift_repaired') + a no-op dispatch. After a month that is ~2,900 junk ledger rows for one payment, the admin pill still reads pending, and reconcile's 'repaired' metric permanently reports phantom work.
    evidence: dispatchStripeEvent.ts:455-458: `.update({ payment_intent_status: 'canceled' }).eq('stripe_payment_intent_id', paymentIntent.id)` - status untouched. reconcile.ts:108-114 selects `.eq('status', 'pending')`; 150-151 maps `pi.status === 'canceled' ? 'payment_intent.canceled'`; 157-166 records drift_repaired BEFORE dispatch; 176 `repaired++` unconditi
    verdict: Every claim verified in code. handlePaymentIntentCanceled (src/lib/payments/dispatchStripeEvent.ts:455-458) updates only payment_intent_status, leaving payments.status='pending'. reconcileStuckPayments (src/lib/payments/reconcile.ts:108-114) selects status='pending' rows past the 15-min SLA with no filter on payment_intent_status, maps pi.status 'canceled' to a replay of payment_intent.canceled (150-151), appends a drift_repaired payment_events r

--- [MEDIUM] No dispute management surface: the disputes table is written by the webhook and read by nothing  => CONFIRMED (high, severity=medium)
    file: src/lib/payments/dispatchStripeEvent.ts:783  category: visibility
    summary: handleChargeDisputeCreated upserts a rich disputes row (amount, status, reason, evidence_due_by) 'so it surfaces in the ops view' - but grep shows no component, hook, or API route reads from('disputes'). The admin gets a single dispute_opened bell notification and then has no screen showing open disputes, their status, or the evidence deadline; charge.dispute.closed updates a row nobody sees. Because tenants are Connect Express accounts, dispute evidence must be handled by the PLATFORM in Stripe - so in-app visibility is the tenant's ONLY window, and it does not exist. A missed evidence_due_by is an automatic loss of the full disputed amount plus the $15 fee, followed by the cleaner clawback.
    scenario: Homeowner disputes a $180 charge. Admin sees one bell ping on a Tuesday, gets busy, and has nowhere in the app to re-find the dispute or its due date (the notification payload is the only carrier). The 7-day evidence window lapses; dispute auto-lost; tenant debited $195; cleaner clawed back; the disputes row silently flips to 'lost' in a table with no reader.
    evidence: dispatchStripeEvent.ts:783-795: `supabase.from('disputes').upsert({ ... evidence_due_by: evidenceDueBy }, { onConflict: 'stripe_dispute_id' })` with comment 'so it surfaces in the ops view'. Grep `from('disputes')` across src/: only dispatchStripeEvent.ts (writer). unmatched_dispute (768-776) goes to payment_events, also unread (see finding 4).
    verdict: Verified. dispatchStripeEvent.ts:783-795 upserts rich disputes rows and :838-841 updates them on charge.dispute.closed, but nothing in src/ ever SELECTs from disputes (only wipe/delete paths reference it), no RPC includes it (payment_stats in migration 077 reads only payments/payouts), and no code ever sets a 'disputed' payment status, so the payments UI shows nothing. The auditor's grep actually missed two realtime subscriptions ON the disputes 
    correction: Two overstatements to correct. (1) The dispute_opened alert is not a transient one-shot bell ping: notification_events is a persistent, paginated per-user feed (useNotifications.ts), and labels.ts:238-246 renders it with the amount and 'respond by {date}', so the evidence deadline IS re-findable in-

--- [MEDIUM] payout.failed / payout.canceled revert bank_paid rows silently: no ledger event, no notification to anyone  => CONFIRMED (high, severity=medium)
    file: src/lib/payments/dispatchStripeEvent.ts:1136  category: notification
    summary: handlePayoutFailed flips affected payout rows from bank_paid back to 'paid' and clears the payout id - console.log only. Unlike every other money-failure path in the dispatcher, it writes no payment_events row and no recordNotificationEvent, so a cleaner whose bank rejected the deposit (closed account, bad routing number) is told nothing, and the admin dashboard shows nothing actionable. The success path notifies (notifyCleanerPaid, 1081-1111); the failure path - the one requiring the cleaner to fix their bank details before Stripe can retry - is mute. The money is not lost (it returns to the connected balance) but the human action needed to release it is never requested.
    scenario: Cleaner closes their bank account after onboarding. Their next $340 payout fails; Stripe fires payout.failed; the DB quietly reverts rows to 'paid'. Cleaner sees 'paid' in-app, assumes a bank delay, and only discovers the problem after a second and third payout also bounce - each silently. Nobody in the org knows to tell them to update their bank details.
    evidence: dispatchStripeEvent.ts:1136-1144: `.update({ status: 'paid', stripe_payout_id: null, bank_paid_at: null })... console.log('payout.failed: reverted ...')` - no recordPaymentEvent, no recordNotificationEvent, and payout.failure_code (logged at 1118) is discarded. Contrast the success path at 1072: `await notifyCleanerPaid(supabase, cleaner.id, payout
    verdict: Verified. handlePayoutFailed (dispatchStripeEvent.ts:1113-1145) reverts bank_paid rows to 'paid' with console logging only: no recordPaymentEvent (used by ~12 sibling handlers at lines 153/359/540/662/719/769/798/847/874/898/920, never here), no recordNotificationEvent, and payout.failure_code (logged line 1118) is discarded. The success path notifies via notifyCleanerPaid (1072/1081-1111). Refutation attempts all failed: (1) the notification cat
    correction: Two minor additions, not corrections: (a) reachability nuance: per the repo docs, the prod live-mode webhook does not yet subscribe payout.paid/payout.failed, so this handler is unreachable in production today; adding those events is an explicit planned cutover step, at which point the gap becomes l

--- [LOW] Reclaiming a failed/stale webhook event does not re-stamp its row, defeating the in-flight window and allowing parallel processing  => CONFIRMED (high, severity=low)
    file: src/lib/payments/webhookIdempotency.ts:60  category: idempotency
    summary: When claimWebhookEvent reclaims via the 23505 path (status='failed', stale 'received', or the row-vanished/select-errored case at line 60 where a transient SELECT failure also yields data=null -> 'claimed'), it never updates the row - status and received_at stay as they were. So a live Stripe redelivery and the dead-letter sweep (which selects the same non-processed stale rows) can process the identical event concurrently, and two simultaneous redeliveries of a 'failed' event both get 'claimed'. Actual money movement is protected by Stripe idempotency keys and the payouts unique index (migration 088), so the residual blast radius is duplicated payment_events/notification rows and doubled Stripe API traffic - but the claim's own PARALLEL-double-processing protection (the 60s window, line 52) only ever guards first deliveries.
    scenario: Event E processing failed at 10:00 (row 'failed'). At 10:15 the cron dead-letter sweep re-fetches and dispatches E; 2 seconds later Stripe's scheduled retry arrives, claimWebhookEvent sees status='failed' -> 'claimed', and dispatches E again in parallel. Both runs execute handlePaymentIntentFailed: duplicate late_payment_failure ledger rows and a duplicate admin authorization_failed notification (no dedupe_key on that call, dispatchStripeEvent.ts:381-386).
    evidence: webhookIdempotency.ts:54-66: the conflict path only SELECTs (`.select('status, received_at')`) and returns 'claimed' for failed/stale rows with no UPDATE re-stamping status/received_at; line 60 `if (!row) return 'claimed';` also converts a transient SELECT error into a claim. reconcile.ts:58-64 selects the same rows for the sweep with no claim hand
    verdict: Verified in full. claimWebhookEvent's 23505 path (src/lib/payments/webhookIdempotency.ts:54-66) never re-stamps the row: a 'failed' row returns 'claimed' unconditionally (line 62) and the 60s in-flight window (lines 65-66) applies only to status='received', so it only guards first deliveries. Two simultaneous redeliveries of a failed event both get 'claimed'. Line 60 destructures away the select error, so a transient SELECT failure also returns '

--- [LOW] payout.paid fallback can stamp the wrong payout row when the precise transfer-id update errors transiently  => CONFIRMED (high, severity=low)
    file: src/lib/payments/dispatchStripeEvent.ts:1058  category: money-correctness
    summary: In handlePayoutPaid, when transfer IDs resolve but the precise UPDATE fails with a DB error, count stays 0 and control falls into markOldestUnattributedPayout - which stamps the single OLDEST 'paid' row lacking a payout id with THIS payout's id, even though the resolved transfer IDs prove which rows actually belong to the payout (and the oldest unattributed row may not be among them). A later payout.failed for this payout id then reverts that wrong row to 'paid' while the genuinely-included rows stay unmarked. The narrowed fallback is correctly conservative for the no-transfer-ids case, but reusing it after a transient error on a known-precise match trades correctness for coverage.
    scenario: Cleaner has payouts A (old, from a legacy transfer never attributed) and B (in today's bank payout, transfer id resolved). The precise update for B hits a transient DB error -> fallback marks A bank_paid with today's payout id. Tomorrow that bank payout fails: payout.failed reverts A (never actually in the payout) and leaves B marked... nothing - B was never marked, so B stays 'paid' forever while A's history is corrupted.
    evidence: dispatchStripeEvent.ts:1049-1063: `if (updateError) console.error(...) else count = ...; if (count === 0) { count = await markOldestUnattributedPayout(...); }` - the error branch and the legitimately-zero-rows branch are conflated into the same fallback. markOldestUnattributedPayout (976-1001) selects `.eq('status','paid').is('stripe_payout_id', nu
    verdict: Verified at dispatchStripeEvent.ts:1049-1063: a DB error on the precise transfer-id update leaves count=0 and falls into markOldestUnattributedPayout (976-1001), which picks the oldest paid/unattributed row for the cleaner with no relation to the resolved transferIds — the structural defect is exactly as described, and it is actually broader than claimed: the same fallback also fires when the precise update succeeds with 0 rows because the reconc
    correction: The defect is real but the scenario is mis-stated in three ways. (1) The trigger is broader than a transient DB error: count===0 also occurs when the precise update legitimately matches 0 rows because the reconcile-payouts route or an earlier event already marked the payout's rows bank_paid — the or
